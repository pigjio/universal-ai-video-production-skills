# 0.5.0 新手引导与阶段落盘改造及验证报告

[中文说明](03_0.5.0_新手引导与阶段落盘验证报告.md)｜[한국어 안내](03_0.5.0_新手引导与阶段落盘验证报告.ko-KR.md)

## 1. 版本定位

0.5.0 是 0.4.0 Seedance 生产融合版的独立后继版本，定位为：**论文方法论外部新手引导与方法执行版**。本次没有覆盖 0.4.0，也没有删除其专业判断；主要将复杂度从用户侧转移到 Agent 内部。

## 2. 已完成改造

### 新手交互

- 总控作为唯一新手入口；用户无需知道十个 Skill 的名称。
- 首次对话支持模糊想法、已有故事/剧本、已有图片、生成效果不对四种自然入口。
- 新手、协作、专业三种模式可切换，默认新手模式。
- 每轮一个小目标、最多一个阻断问题；“不知道”是合法回答。
- 首轮必须给具体小产出，不允许只发问卷。
- Agent 推荐、用户积极反馈、阶段完成、保存、生产批准、人工接受与研究同意分别处理。

### 阶段成果落盘

十个 Skill 均加入阶段主文件和同一落盘事务：

```text
完整阶段成果展示
→ 用户明确确认阶段完成
→ 检查本轮或项目级保存授权
→ 创建新的完整Markdown版本
→ 回读主文件
→ 更新并回读PROJECT_INDEX.md
→ 更新并回读SESSION_HANDOFF.md
```

规则明确禁止：只保存摘要、静默覆盖旧版、写入失败却声称成功，以及把本地保存外推为上传、生成、发布或研究同意。

### 跨会话与多入口

新增 `START_HERE.md`、`AGENTS.md`、`CLAUDE.md`，以及 `PROJECT_BRIEF.md`、`PROJECT_INDEX.md`、`SESSION_HANDOFF.md`、`DECISIONS.md`、`AUTHORIZATIONS.md` 和阶段成果模板。新会话先核验授权、索引、交接和实际文件，不凭记忆猜最新版。

### 三层深度

- production：普通创作者默认；
- provenance：来源追溯与方法审查按需；
- research：必须明确单独启用，默认未授权，且不得反向修改 production 确认状态。

## 3. 静态验证

运行：

```bash
python evals/validate_package.py .
```

结果：

- Skills：10
- References：88
- 公共 schema 示例：34
- Errors：0
- Warnings：0

验证覆盖：Skill 名称与 0.5.0 版本、两套公共协议的十份副本一致性、正文路由、阶段落盘标记、必需项目模板、递归资源、YAML 示例、研究默认未授权和私有项目/路径泄漏。

静态报告：`evals/static-validation-0.5.0.json`。

## 4. 实际 Agent 文本/文件工作流

本轮保存了三条代表性实际运行：

1. **冷启动**：对“我想做一个AI动画，但还不清楚是什么”先给创意小产出，不展示 Skill 清单、不落盘项目成果。
2. **阶段确认后落盘**：在有效持续保存授权下，新建完整 `剧本_v002.md`，保留 `剧本_v001.md` 哈希不变，并实际更新、回读索引和交接；研究、上传和付费权限保持 denied。
3. **能力降级**：无 Seedance 工具时仍交完整请求骨架与外部回传清单；单张截图不能证明声音或动作连续性，相关项保持 `unconfirmable`，不伪造任务 ID 或生成结果。

确定性检查：11/11 通过，见 `evals/novice-run-check-0.5.0.json`。实际输出位于 `evals/runs/novice-0.5.0/`。

## 5. 尚未验证

以下不能由本轮静态和三条代表性测试证明：

- `evals/novice-cases-0.5.0.md` 中全部 14 条情形尚未逐条运行；
- 真实 Seedance 平台提交与返回媒体；
- 多随机种子下的稳定性或成功率提升；
- 外部创作者第一次使用 Codex、Claude Code 或其他 Agent 的实际完成率；
- 艺术质量提升和真实人工接受。

因此当前可称：**0.5.0 候选包结构完成，静态验证通过，三条代表性 Agent 文本/文件路径通过**；不能称平台验证、统计稳定性或外部新手验收完成。

## 6. 回滚和版本边界

0.4.0 源目录保持不变。0.5.0 使用独立目录、独立交付副本和独立 ZIP。`CHANGE_MANIFEST.json` 不纳入自身哈希，避免自引用失效。
## 7. 审查后补强（2026-09-10）

根据新手可用性审查，0.5.0 包内协议和十个 Skill 已补充两项行为约束：

1. 阶段成熟后统一使用一次收尾问题，提供“确认并保存、确认但暂不保存、继续修改”，避免把确认与保存拆成两轮重复询问。
2. 空项目首次保存时，目录明确且保存授权有效后，由 Agent 自动创建缺失的五个基础记录文件，不要求新手复制模板或填写内部字段。

已增加 N15“空项目首次保存与自动初始化”测试规格，并在静态校验中增加相应一致性断言。补强后已重新运行静态校验：10 个 Skill、88 份 references、34 条 schema 记录检查均为 0 错误、0 警告；原有 11 项代表性工作流确定性检查仍为 11/11。N15 尚需保存真实 Agent 文件工作流证据后，才能计入 Agent 文本/文件层通过。
