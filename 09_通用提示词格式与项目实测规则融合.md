# 通用提示词与 Seedance 生产方法融合｜0.5.0

> 0.5.0 保留 0.4.0 的正式请求与生产方法，并增加新手逐轮引导、阶段确认后完整 Markdown 落盘、版本保护和跨会话恢复。

本文件是快速索引。完整执行以对应 `SKILL.md` 和条件 references 为准，不另建冲突的第四套规则。

## 核心分工

| 阶段 | 主要产物 | 关键控制 |
|---|---|---|
| 分镜 | shot 与导演观看任务 | 景别、机位、调度、动作因果、切点 |
| Prompt 合同 | 自包含 generation request 文本 | 实际素材职责、逐镜五字段、可见终点、接口 |
| 视频生产 | production group、request、真实状态、edit unit | 能力证据、提交回执、尾帧/文字续接、剪辑来源 |
| 生成评审 | 十维证据、修复层级、尾帧资格 | 症状≠根因、最小试验、回归、人工裁决 |

## Seedance 正式提交结构

一个独立请求一个闭合代码块，按需包含：请求窗口与画幅、影片风格与持续状态、唯一任务/完成判据、主体边界、实际上传素材、纯文字约束、场景/状态边界、逐镜五字段、真实续接接口和少量高风险限制。

逐镜五字段为：

1. 参考图锚点；
2. 镜头任务；
3. 构图与动作；
4. 摄影机与注意力；
5. 声音/转场/切点。

第一镜建立起点，后镜继承结果，不重演已完成动作。

## 参考素材

每项写“实际句柄—已观察事实—当前读取范围—锁定职责—禁止继承”。文件存在、视觉确认、实际上传分别记录。复合图按上传文件计数，内部面板只作职责锚点。无图对象/特效使用正向文字生命周期，不伪装成 `@素材`。

## 生产分组

按共同任务、空间、主体状态、连续动作链和参考集合建立 `production_group`；Clip/DO 只作显示别名。然后根据当前平台能力映射为一个或多个 `generation_request`。长窗、短段、桥接和 pickup 都是候选，不以固定秒数决定。

## 平台能力与证据

所有时长、图/视频/音频数量、输入模式、声音和字符容量都记录来源 URL、核验日期、模型、产品入口与模式。官方明确事实、官方案例归纳、第三方经验、项目观察和人工接受基线不能混写。

## 十维评审

身份、空间、几何、动作、方向、道具、摄影、信息边界、声音、尾帧逐项按适用性记录要求、证据、范围和状态。身份漂移只是症状；先查实际绑定、素材冲突、遮挡/尺度、动作/运镜负荷、原片/后期和随机性。

原片正确且后期引入问题→后期；提交可修且导演意图不变→重生成；必须改变镜头设计或观看任务→退回分镜；只有源资产自身矛盾才退资产层。

## 外部独立性

外部使用者不需要访问原生 `ai-video-production`、作者项目或个人知识库。本包内 references 已承载迁入的方法。项目 Canon、固定角色/空间、个人路径、固定 DO 编号和历史平台参数不进入通用层。
