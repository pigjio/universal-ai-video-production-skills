# 从这里开始

你不需要知道任何 Skill 名，也不需要先学提示词。直接用自己的话开始，Agent 会选择合适方法，并一次只推进一个小目标。

可以使用中文、韩文或其他自然语言。Agent 默认跟随你当前使用的语言；切换语言不会改变工作阶段、证据标准或授权边界。

## 任选一个入口

1. **只有模糊想法**：例如“我想做一支关于告别的短片，但还没想清楚”。
2. **已有故事或剧本**：粘贴文字或提供文件，再说最想先解决的问题。
3. **已有图片**：提供图片，并说“想保留什么、想改变什么、最后想用来做什么”。
4. **生成效果不对**：提供生成结果和原提示词；只会说“不像我想的”也可以。

推荐开场：

> 我是新手。我现在有【一个想法 / 故事或剧本 / 图片 / 不理想的生成结果】。请一次只带我做一步；我不知道时给我容易比较的选项。

韩文开场：

> 저는 처음 사용합니다. 지금은 【아이디어 / 이야기나 시나리오 / 이미지 / 만족스럽지 않은 생성 결과】가 있습니다. 한 번에 한 단계씩 도와주세요. 제가 모르겠다고 하면 비교하기 쉬운 선택지를 주세요.

## 你只需要做什么

你只需要：**描述、比较、否决、确认**。术语、步骤、文件格式、版本和技能路由由 Agent 管理。“不知道”是有效回答。

Agent 默认采用新手模式。你也可以随时说：

- “简单一点”——继续新手模式；
- “和我一起比较方案”——协作模式；
- “给我专业字段和生产交接”——专业模式。

## 确认与保存

讨论、探索、正式成稿、生产交接是不同阶段。Agent 的推荐和你的积极反馈都不自动等于确认。

当某阶段完整成果已经展示，Agent 会主动用一个问题让你选择：**确认并保存、确认但暂不保存、继续修改**。你不必记固定口令；也可以明确说：

> 我确认本阶段完成。请保存为新的 Markdown 版本，不覆盖旧版。

项目首次可选择授权：**今后每个阶段在我明确确认后，自动保存一个新的本地 Markdown 版本**。这项授权不包括覆盖、删除、上传、付费、发布或研究使用。

第一次需要保存时，你只需给出项目文件夹。Agent 会自动创建缺失的项目简报、授权、索引、会话交接和决策记录；你不需要复制模板或填写内部字段。保存成功必须经过回读验证，并同步更新项目索引和会话交接；失败时 Agent 必须如实说明。保存作品不等于同意用于研究，研究默认关闭。

## 下次继续

项目模板会记录已确认版本、当前候选、授权和下一步。新会话只需说：

> 请先读取 PROJECT_INDEX.md 和 SESSION_HANDOFF.md，告诉我已经确认到哪里，然后一次推进一个小目标。

相关规则：`shared/novice-guidance-protocol.md`、`shared/common-contract.md`。项目文件模板位于 `templates/`。
