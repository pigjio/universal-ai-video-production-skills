# 通用 AI 影像制作 Skills 0.5.0｜十技能审查汇编
> 本文由当前十个 `SKILL.md` 自动汇编。新手入口见 `START_HERE.md`；统一协议见 `shared/novice-guidance-protocol.md` 与 `shared/common-contract.md`。

---

## ai-character-design

---
name: ai-character-design
description: "角色设计创作搭档与生产封装；新手自然地说‘帮我想一个角色’‘这个人看起来太僵’‘强大但胆小怎么画’也触发，无需完整角色包或母版。Use for guided character ideation, critique, silhouette, proportions, identity, expression, pose, costume, non-human casts, consistency and handoff without replacing story, world or rig validation."
version: 0.5.0
author: Domain Knowledge Distillation
license: MIT
metadata:
  hermes:
    tags: [character-design, narrative-animation, visual-development, character-consistency, ai-image, ai-video, production-handoff]
    related_skills: [ai-screenplay-development, ai-worldbuilding, ai-scene-design, ai-storyboard-design, ai-prompt-execution-contract, ai-generation-review]
---

# AI 影像角色设计｜完整可执行封装

## 新手引导与阶段成果落盘

本阶段开始时显式读取 `references/novice-guidance-protocol.md`；涉及正式状态、授权、版本、证据或交接时同时读取 `references/common-contract.md`。默认采用**新手模式**，可随时切换协作或专业模式；只调整术语和讲解密度，不删减下文的角色设计、生产判断与闸门。

阶段落盘必须保存完整正文，并在写后回读通过后更新 `PROJECT_INDEX.md` 与 `SESSION_HANDOFF.md`；这三个文件及其状态必须相互一致。

- 每轮只推进一个小目标，最多问一个真正阻断当前目标的问题；其他缺口写成可替换假设。首轮先给角色判断句、可见动作、剪影/形状提案或一项具体诊断，再问至多一个问题，不得只发角色问卷。用户可回答“不知道”，随后采用明确标注的推荐假设继续。
- “推荐这个”“不错”“可以”“继续”只更新偏好，不构成阶段确认。仅当用户明确说“阶段完成”“按此定稿”或同等无歧义表述时记录人工确认；角色内核、身份锚点、母版和生产通过等关键裁决不得自动代批。
- 阶段稿成熟时先给可独立阅读的完整角色设计正文。项目已有一次性“阶段确认后保存”授权时，明确确认后直接写新版本；否则把确认与保存合并为一个问题，主动提供“确认并保存为新的 Markdown 版本 / 确认但暂不保存 / 继续修改”三种自然选择。用户选择保存且目录明确后，自动创建缺失的项目基础记录文件，不要求新手手工复制或填写内部字段。阶段主文件固定为 `角色设计_名称_vNNN.md`，名称取用户确认的安全文件名；未知时可用明确暂名而不得阻塞早期创作。写前扫描该名称的同名文件，以最大三位版本号加一，首次 `v001`，绝不覆盖；保存约定范围内完整的角色判断、视觉/动作系统、已定/暂定/禁变项、风险与下游接口，不以摘要或修改清单代替正文。
- 写后回读核对，再更新 `PROJECT_INDEX.md` 的当前版本/状态/链接和 `SESSION_HANDOFF.md` 的人工决定、母版/未决项、下一步，并回读关键更新。任一步失败报告实际完成范围，不得声称保存成功；项目目录未知时只问该阻断问题。
- 保存不等于阶段确认、母版/Canon、生产批准或 research 同意，也不授权联网、采集、上传、付费、生成或发布；正式资产登记和交接继续执行 `references/common-contract.md`。

## 1. 目标与边界

讨论直接答本轮问题；探索可保留未知和可替换假设，不要求完整公共头、落盘或生产闸门。进入正式资产记录、生产交接/验证或处理已登记资产的上游改版时加载 `references/common-contract.md`，遵守授权写入、真实证据与依赖复核规则；独立登记产物使用 `references/state-version-and-templates.md` 的完整公共头，局部字段是 payload。

支持 narrative / perceptual / conceptual。无角色作品允许 `character: null` 并跳过造型流程；有角色但无人物冲突时，用“感知功能—身体机制—可观察动作—完成判据”替代 want/fear/flaw/弧线，相关闸门仅检查适用项。以下数量与姿态类型为完整叙事角色的探索建议，不是所有项目的最低硬配额。

把文字角色转成一个**可读、可动、可复现、可建模、可绑定、可表演、可交接**的视觉演员系统，而非只产出一张好看的概念图。

```text
角色资产 = 叙事或感知功能 + 适用的内在动机 + 核心动词
         + 形状/剪影 + 体块/比例 + 头部/表情 + 动作身份
         + 服装/道具 + 色彩/材质 + 生产约束
         + 权威母版 + 派生关系 + 版本状态
```

本技能负责角色策略、概念探索、设定规格、参考职责、母版与派生管理、生产验收和交接。它不替代：

- `ai-screenplay-development`：故事结构、人物弧线与场次写作；
- `ai-worldbuilding`：时代、文化、技术、物质与社会规则；
- `ai-scene-design`：空间拓扑、环境母版与机位；
- `ai-storyboard-design`：镜头调度、构图与连续性；
- 最终拓扑、UV、shader、绑定控制器、动画曲线或视频生成执行。

快速方案或局部讨论只交本轮需要的判断、具体提案和关键代价，不强制最小成功集。正式成稿只完成用户指定范围；明确要求完整角色包才执行适用的全部阶段，生产验证另以真实证据判定。

## 2. 运行原则

1. **功能先于造型**：叙事、感知或概念目标中的抽象形容词必须落为可观察视觉证据。
2. **大形先于细节**：先剪影、动态线、负形、体块，再服装纹理与渲染。
3. **角色是动词**：设计默认站姿、压力反应、步态与核心动作，不只设计名词身份。
4. **探索与锁定分离**：候选是候选，母版是母版；人工未裁决前不可混合成“最终版”。
5. **参考各司其职**：一张参考只承担明确职责，不允许用配色图改比例、用姿态图改脸。
6. **风格探索与锁定派生分开**：新风格探索可改比例、体块和图形骨架；已锁母版派生保护约定锚点。超界变更另开候选并请求裁决，不静默覆盖母版。
7. **生产前必须量化**：概念阶段可用范围；正式生产用 turnaround、lineup 或建模资料须锁唯一标尺，讨论和比例探索不提前强锁。
8. **最小回退**：哪里失败退回哪里，不用材质遮剪影问题，不用 prompt 加长遮结构问题。

## 3. 启动模式与停止条件

先判断 interaction_mode（仅为对话概念，不增改公共 YAML 字段）：**讨论**直接判断或试写；**探索草案**比较可替换候选，不假装批准；**正式成稿**完整交用户指定范围；**生产交接/验证**才适用证据闸门。探索候选不是正式可交接的 candidate 记录，输入不足保持草案。共同循环：当前意图 → 直接判断/具体试写 → 必要时少量替代与代价 → 吸收反馈 → 只改受影响项；允许不收敛。内部保留已定、暂定、否决原因和开放项，不每轮强制列表。再选择任务分支：

| 模式 | 典型请求 | 默认交付 | 何时停止 |
|---|---|---|---|
| 快速方向 | “给我三个角色方向” | 按要求给有区别的方向及代价，不强制核心卡/评分表 | 本轮问题已回应，可不选定 |
| 从零设计 | 从剧本建立角色 | 草案或指定范围成稿；完整包才走完整主流程 | 约定范围完成，不冒称生产通过 |
| 局部深化 | 表情、姿态、服装、三视图 | 可用暂定基线探索；正式母版派生才须锁定来源 | 局部需求已回应；验证任务才检查专项闸门 |
| 诊断返修 | 跑脸、比例漂移、不可动 | 竞争根因 + 最小修复；缺图则标假设 | 讨论给出判断；实际返修再做回归测试 |
| 既有资产整理 | 多张已通过角色图 | 权威关系 + 固定卡/档案卡 | 无冲突、无裁切、职责明确 |
| 群像/非人分支 | cast、动物、怪物 | 只加载当前问题的分支；完整包才联动主流程 | 讨论问题已回应；生产验证才检查类型闸门 |

只在缺失信息会改变**故事功能、物种/身体机制、受众安全、生产目标或现有母版权威性**时追问。其他缺口用“可替换假设”推进。用户要求逐问时，每次只问一个高影响问题；建议顺序：第一感觉 → 欲望/恐惧 → 核心动词 → 弧线终点 → 主记忆点 → 主形状 → 生产目标。

## 4. 输入契约与缺口分级

完整角色包的输入参考（不是每次对话的必填表）：

```yaml
project:
  medium: short_animation | short_film | series | other
  audience: ""
  tone: ""
  visual_style: ""
  production_target: concept | ai_image | ai_video | 3d | rig | animation | storyboard
  constraints: []
story:
  logline: ""
  theme: ""
  world_rules: []
character:
  name: ""
  narrative_function: ""
  want: ""
  fear: ""
  flaw_or_contradiction: ""
  relationships: []
  key_scenes: []
  core_verbs: []
  species_or_body_mechanism: ""
  required_props: []
  forbidden_directions: []
existing_assets:
  files: []
  current_master: ""
  latest_human_ruling: ""
```

缺口分级：

- **P0 阻断当前定稿/交接**：缺口确实影响所请求范围的功能、媒介、母版权威或文化语境时，暂停该范围的正式结论；仍可直接讨论不受影响部分、给暂定方案。不得因媒介未定阻断一次笑容讨论，也不凭空填文化事实。
- **P1 可假设**：受众、风格、部分关系或动作未定。列假设并继续到方向稿，不进入生产锁定。
- **P2 后补**：文件命名、次要表情、纹理微差。不中断核心设计。

如上游已更新，先做 delta 同步：记录新版本 → 搜索旧决策/待定项 → 标出直接及传递受影响资产为 needs_recheck；历史批准保留但不充当当前有效门禁。重审后才恢复 confirmed，无关依赖须记录保留理由。详见 `references/state-version-and-templates.md`。

## 5. 完整主流程

以下为完整包的模块库，不要求讨论逐阶段执行；只加载受当前问题影响的阶段。无实际素材/观察者时写设计预估，不冒充观众反馈。讨论与草案不以阶段闸门或完整生产测试为停止前提。

### Phase 0｜建立任务账本

**输入**：用户描述、剧本/世界规则、现有素材。  
**动作**：确定模式、目标媒介、镜头尺度、交付深度、权威文件、缺口与风险。为每张现有参考登记“贡献/不得覆盖”。  
**输出**：Input Sheet、Assumption Log、Reference Responsibility Table、当前状态。  
**闸门 G0**：P0 清零；否则不得声称已可定稿。

### Phase 1｜角色内核与设计命题

写出：

```text
这是一个[故事功能]；因为[欲望/恐惧/缺陷]，
习惯通过[3–7 个核心动词]处理压力；
观众第一眼应感到[目标感受]；
因此视觉上以[主形状、比例、姿态、表情、身份物证、色彩]证明。
```

输出 Character Core Card：story role、want、need、fear、矛盾、默认社交策略、压力反应、角色弧线、3–5 个 design pillars、3 个禁止方向。把“勇敢、内向、聪明”等词改写为胸腔方向、步幅、视线、手势、节奏、磨损等证据。

**闸门 G1**：去掉服装仍有性格；换成另一角色时，其动作策略必须改变；每个视觉支柱都能追溯到故事功能。失败退回角色内核，不开始画细节。

**讨论判断**：职业相同不等于策略相同；用同一压力下的不同动作反证设计观点，而非靠装备区分。遇到“强大但胆小，不要萌化”先保留力量体块、改避险姿态与视线。加载 `references/design-foundations.md`「观点与反证」。

### Phase 2｜功能型参考研究

建立四类参考：故事/文化证据、结构/运动机制、风格/渲染语法、禁止/反俗套。每张参考填写：

```text
提取：形状/比例/动作/材质/剪裁/行为中的哪一项
应用：角色哪个模块
不提取：哪些脸、比例、IP 特征或文化符号
风险：侵权、刻板印象、结构不可信、下游复杂度
```

不要做无注释图片堆。未经核验的文化符号不能当“氛围装饰”。

**闸门 G2**：关键视觉决定均有功能依据；不存在“整张照抄”；禁止方向可被操作性检查。

### Phase 3｜五维图形骨架与方向探索

定义并逐维验收：

1. **外轮廓**：唯一主导形；副形只制造 unity/variety/contrast。
2. **动态线**：C/S/Z、前倾、后缩或断裂；说明势能方向与张力是凝聚还是释放。
3. **内切割**：头/躯干/四肢/服装大块面比例和第一视觉焦点。
4. **具象记忆点**：1–3 个，必须绑定故事或行为；可删而不损角色者只是装饰。
5. **剪影性格**：第三人只看黑影时的第一情绪。

从零探索可比较比例、姿态、形状联动的完整方向；诊断故障才优先只改 1–2 个变量以便归因。12–40 个小剪影或低成本 6 个仅是预算示例，不是配额。不得把多个候选平均融合。评分按需使用 `references/design-foundations.md`。若感觉机械，先查等大、平行、等距和切线粘连，区分有意秩序与无意僵硬，再改一个关系比较焦点；不强制不对称，加载其中「等距与机械节奏」。

**闸门 G3**：邮票大小 1–3 秒可读；黑影能读角色功能/性格；负形不粘连；与主要角色不混；五维指向同一内核。失败只退回大形。

### Phase 4｜体块、比例与可复现公式

用 3–7 个主质量块定义 skull/head、胸腔、骨盆、四肢、手脚；标 centerline、眼线、肩线、髋线、重心、支撑面、关节点与附件挂点。建立：

```yaml
construction_formula:
  standard_pose_and_total_height: ""
  head_to_body_ratio: ""
  head_chest_pelvis_masses: ""
  shoulder_hip_relation: ""
  limb_rhythm_and_hand_foot_scale: ""
  center_of_gravity: ""
  joint_and_socket_points: []
  asymmetry: []
  deformation_rules: []
  invariant_silhouette: []
```

正/侧/背/3/4 应由同一体块推导，不能分别“画得像”。生产锁定时明确单位、姿态、端点和曲线路径，清除“约、范围、到腰”等冲突描述。

**闸门 G4**：可直接搭 3D blockout；各视图体积闭合；极端姿态仍像同一角色；比例解释年龄、力量与运动方式。失败退回体块/标尺，不靠发型遮掩。

### Phase 5｜动作身份与身体表演

从关键场次提取 5–10 个 core verbs，定义 neutral、pressure、action、social、failure 五类姿态，以及默认 gait、leading body part、energy quality、节奏与道具交互。每个姿态说明动词、态度、动态线、重心、支撑/接触状态与负形；腾空等相位可无当前支撑点，改标此前发力和后续接触。

用动画思维设计表演 beat：`notice → interpret → decide → act → react`，为眼神、头颈、肩髋、手、附件和表情残留分配动作。

动作判断沿意图 → 力入口 → 峰值 → 释放 → 锚点 → 附件响应 → 前后姿态检查，不只画一根动态线。Force 是表演启发而非力学认证，详见 `references/performance-and-production.md`「完整 Force 路径」。

**闸门 G5**：遮脸仍能读出动词；静态承重检查支撑区，奔跑、跃起、跌倒等动态失衡检查起势、惯性、接触转换和落地/恢复路径，不强制瞬时重心在脚下；相似情绪场次有可见差异；道具有携带、使用、收纳及移动状态。失败退回 force map/姿态，不加夸张脸补救。

### Phase 6｜头部、表情与手部系统

先区分结构静止位、角色习惯脸与即时表情；讨论可用暂定基线，正式派生须引用已锁来源。去掉发型检查 skull、颧骨、眼窝、鼻楔、口轮、下颌、耳位和颈部连接。识别问题优先比头形、左右眼、鼻、嘴五形的距离/大小/角度及 T 形关系，标准比例是比较尺而非平均脸目标，加载 `references/design-foundations.md`「五形与 T 形」。完整表情系统按需要覆盖基础情绪、专属反应、体力状态及强度；局部提问只做所需差量。手部定义尺度、职业痕迹、手势、持握和 rig 风险。

正式生成式派生按小模块逐项批准，Face ID → 按需表情组 → 头部多角度 → 手/道具 → 全身姿态；不固定格数。复合表情先问真实情绪与社交遮掩，再给眉眼/口部分工，允许有意冲突，比较易混读法而不平均两张脸。加载 `references/performance-and-production.md`「三层表情基线」「复合与压抑表情」。

**闸门 G6**：去发型仍可识别；习惯脸偏置有意且即时情绪差量可读；检查项目相关易混读法，不凭笑容推断真伪；强表情不换脸；脸部与身体共同支持表演意图，允许社交遮掩造成局部冲突。

### Phase 7｜服装、发型、道具与身份物证

每个主要元素回答：来自哪里、谁制作、使用多久、为何保留、如何穿戴/收纳、如何影响动作与剪影。先设计肩线、腰线、衣摆、鞋帽等大结构，再加接缝、磨损和纹理。道具是“第二演员”，必须测试持握、挥动、坐下、跑动、收纳和碰撞。

**闸门 G7**：元素同时证明身份/世界规则/行为之一；不遮挡核心表演区；技术材料符合世界上限；长发、披风、裙摆、绳带、硬饰品均有模拟/绑定方案。失败删减或重构，不用装饰解释人格。

### Phase 8｜明度、色彩、材质与风格化

先灰阶分组，再定主色/辅助色/强调色；在需要的日夜、逆光、低光和群像背景中测试分离。材质说明表面、尺度、粗糙度、磨损、年龄与身份，不把随机纹理当丰富度。新风格探索允许选择性夸张比例、体块、图形骨架；已锁母版派生只改授权变量，不默认局限三级细节，也不越权改身份。比较克制、中度、强图形候选的识别、动作和制作代价；超界另开版本，加载 `references/design-foundations.md`「风格探索与锁定派生」。

**闸门 G8**：灰阶可读；视觉焦点不被纹理抢走；角色与背景有稳定分离；同一 cast 共享 visual grammar 但主次层级不同。

### Phase 9｜特殊分支与群像

按需进入：

- **动物/非人/怪物**：先真实骨架、运动、感知器官与生态，再定拟人等级；耳尾翼触角进入表情。混合生物按“主物种负责骨架与识别、次系统补功能、材质/植物负责变化规律”设计，禁止器官清单拼贴。
- **群像**：先提取系列 DNA，再用身高、主形、体块、姿态、明度、职责物证产生差异；主角专属锚点不得下放给群众。
- **关系动作**：同一刺激下比较不同策略，以视线、朝向、距离和接触组织双人无脸姿态，再回查单体能否表演；不擅改剧本关系，最终调度交分镜。加载 `references/branches-and-diagnostics.md`「群像关系动作」。
- **既有资产/固定卡**：先指定唯一造型母版、Face ID、头部转面、配色材质、服装、手/道具、姿态的职责层级；已通过素材用确定性排版，不让生成模型重新融合。
- **多状态角色**：共同身份锚点保持；每个状态单独列触发、变项、禁变项、回归状态与派生 ID。

读取 `references/branches-and-diagnostics.md` 执行专项规则：混合生物必须闭合自然规律→结构连接→运动中间态→剧情/感知功能；系列先观察母版并做共享/专属校准；生成意外先测功能断点，不能仅因偏离文字而修回。

### Phase 10｜生产验证与角色包交付

执行四层验收：

- **A 概念通过**：内核、剪影、五维、禁止方向；
- **B 结构通过**：比例标尺、体块、turnaround、极端姿态；
- **C 表演通过**：neutral、表情盲测、姿态/动词、手和道具；
- **D 生产通过**：群像/背景可读、材质、关节范围、模拟、LOD、交接完整。

方向通过不等于生产通过。只交付用户实际需要的层级，但必须明确“尚未验证项”。完整闸门、故障树与下游清单见 `references/performance-and-production.md`。

## 6. AI 生成与迭代控制

把 prompt 当“当前阶段的执行契约”，不要把整份角色包一次塞进模型。

```text
任务类型 + 暂定基线或当前唯一母版/参考优先级
+ 本轮变量（诊断优先 1–2 个；探索可联动）
+ 必须保持的结构锚点
+ 构图/视角/输出规格
+ 禁止新增与禁止覆盖
```

迭代规则：

1. 探索轮只比较方向，不锁随机细节。
2. 每轮写 hypothesis（为何改）、delta（改什么）、evidence（如何判断）。
3. 故障诊断优先一轮一层以便归因；探索允许内核、大形、体块和表演联动，但记录差异，不把联动探索伪称单变量实验。
4. 选中的意外结果只有在增强目标功能且不破坏生产时才升级为设计；依 `references/branches-and-diagnostics.md` 检查结构、动作、记忆点与双重读取，先保留候选再记录人工裁决。
5. 若跑脸/比例漂移，减少参考冲突、缩小任务、提高母版权重；不是继续堆形容词。
6. 生成正式母版派生的 turnaround、表情或姿态前，引用已通过母版 ID 和锚点；讨论/探索可用暂定头脸或体块，明确非批准资产，不伪造来源 ID。

## 7. 状态、版本与派生协议

正式登记的独立资产采用以下公共头与专业 payload；讨论和未齐输入的探索不强制套表（完整模板见下方 reference）：

```yaml
artifact_id: CHR-example
version: v1
artifact_type: character
creative_mode: narrative
subject_type: character-led
lifecycle_status: draft
production_status: not_applicable
approval_level: none
upstream_versions: {}
confirmed_evidence: []
open_questions: []
recheck_trigger: []
next_action: request_review
payload:
  parent_id: ""
  source_master_id: ""
  module: core | silhouette | construction | face | expression | pose | costume | material | turnaround | lock-card
  invariants: []
  variables: []
  forbidden_changes: []
  human_rulings: []
  change_log: []
```

- 主版本：内核、物种、主比例或身份体系改变；旧派生全部回退审核。
- 次版本：服装状态、发型系统、表情/姿态模块扩展；受影响派生重审。
- 补丁：不改语义的小修、排版或标签更正。
- 批准只用公共 approval_level；生成进度与生命周期分别记录，`final` 不作为无条件状态。
- 上游变化后做影响传播；已废弃资产保留原因，不再作为模型参考。

模板与最小记录见 `references/state-version-and-templates.md`。

## 8. 输出规格

### 完整角色概念成稿的最小成功集

仅当用户要求完整角色概念成稿时检查以下适用项；讨论、快速方向及局部正式模块按本轮范围完成，不补整包：

1. 角色判断句；
2. 叙事适用时的 want / fear / flaw_or_contradiction，否则感知/概念功能；
3. 3 个 design pillars 与禁止方向；
4. 主形、动态线、比例、剪影锚点；
5. 默认姿态、适用的压力反应、覆盖需求的核心动词；
6. 头脸锚点与表情范围；
7. 服装/道具的功能证据；
8. 明度、色彩、材质方向；
9. 不可变/可变/禁止改变；
10. 建模、绑定、动画、镜头风险与当前批准级别。

### 完整 Character Design Packet

按需包含：Brief、Core Card、Reference Function Board、五维图形表、Silhouette Sheet、Construction Formula、Scale Chart、Turnaround、Face ID、Head Sheet、Expression/Mouth/Hand Sheet、Pose/Motion Sheet、Costume/Prop/Material Key、Cast Lineup、Do/Don’t、Model/Rig/Animation/Storyboard/Scene Handoff、Risk Register、Version Manifest。

输出必须区分：**已知事实 / 可替换假设 / 设计提案 / 人工裁决 / 待验证项**。

## 9. 快速故障定位

| 症状 | 根因层 | 最小回退 |
|---|---|---|
| 好看但不像角色 | 内核 | 重写功能句、矛盾和动词 |
| 缩小像路人 | 图形骨架 | 主形、负形、记忆点 |
| 多角度变人 | 体块/头骨 | construction + Face ID |
| 动作像人偶 | 表演 | 动词、动态线、重心、支撑 |
| 表情只换嘴 | 表情系统 | neutral 与全脸联动 |
| 换装后认不出 | 身份锚点太弱 | 强化身体/头部/姿态锚点 |
| 每轮越改越乱 | 版本/参考职责 | 选唯一母版，限制本轮变量 |
| 很酷但不能做 | 生产约束 | 关节、模拟、道具三状态 |
| 群像同脸同体 | 系列变量 | 头骨/比例/主形矩阵 |
| 角色被背景吃掉 | 明度/材质 | value separation，减纹理 |

复杂症状读取 `references/branches-and-diagnostics.md`，不得直接“重做全部”。

## 10. Reference 路由

- **大形、形状语义、五维一致性、比例、参考评分** → `references/design-foundations.md`
- **动作、表情、三视图、建模/绑定/动画交接、四层验收** → `references/performance-and-production.md`
- **动物/混合生物、群像、多状态、既有资产、AI 漂移故障树** → `references/branches-and-diagnostics.md`
- **状态机、版本、资产 manifest、Core Card 与交接模板** → `references/state-version-and-templates.md`
- **知识来源、覆盖矩阵与适用边界** → `references/source-map.md`

典型任务只需本文件；遇到专项分支或需要长表时再读取对应 reference。本包的执行不依赖外部知识库或绝对路径。

## 11. 最终自检

讨论/探索先检查是否直接回应当前意图、保留否决原因、只改受影响项且区分假设与证据；不要为满足下列完整包/交接清单而补无关产物。以下仅检查所请求范围的适用项。

- [ ] 边界与生产目标明确，P0 已清零。
- [ ] 内核、五维、比例、表演、服装和色彩互相支持。
- [ ] 候选、母版、状态变体和派生资产未混写。
- [ ] 所有参考有明确职责和优先级。
- [ ] 通过级别有证据，未验证项被标出。
- [ ] 失败能定位到最小回退层。
- [ ] 下游无需猜测比例、锚点、动作、材质或风险。
- [ ] 文件中没有项目 Canon、个人导演风格、平台私有参数或外部绝对路径依赖。

---

## ai-creative-workflow-router

---
name: ai-creative-workflow-router
description: "AI 影像创作的唯一新手入口与跨阶段总控；用户只需自然地说‘我有个画面但不知道怎么开始’‘帮我做短片’‘接下来做什么’或直接描述需求即可触发。Use for novice onboarding, project routing, stage coordination, Seedance/即梦 production handoff and recovery while preserving versions, evidence and human approval."
version: 0.5.0
author: Domain Knowledge Distillation
license: MIT
metadata:
  hermes:
    tags: [ai-animation, creative-workflow, routing, production, review, handoff]
    related_skills: [ai-visual-ideation, ai-screenplay-development, ai-worldbuilding, ai-character-design, ai-scene-design, ai-storyboard-design, ai-prompt-execution-contract, ai-video-production-classroom, ai-generation-review]
---

# AI 影像创作流程总控

## 使用说明与唯一新手入口

本技能是整套创作搭档的**唯一新手入口**。首次接手项目时显式读取 `references/novice-guidance-protocol.md`；涉及状态、授权、版本、证据或交接时同时读取 `references/common-contract.md`。用户无需知道 Skill 名、Canon、`artifact_id` 或其他内部术语，只需自然描述想做什么；总控负责翻译和路由，不把内部表格反抛给新手填写。

首次响应先给一个贴合用户描述的具体小产出，再以自然语言提供四种起点：**① 只有模糊想法，② 已有故事或剧本，③ 已有图片，④ 生成效果不对**。允许用户不选编号而直接描述，也允许回答“不知道”；不得展示 Skill 清单或先用一串问题阻断创作。用户询问说明书时再读取 `references/user-guide.md`，按需给说明和链接，不要求先读完。

跨会话恢复时，优先读取项目目录中的 `PROJECT_INDEX.md` 与 `SESSION_HANDOFF.md`，再按其中链接读取必要的当前阶段文件；文件缺失或内容冲突时如实说明，不凭聊天印象伪造状态。项目记录分为 **production / provenance / research** 三层，未特别说明默认只做 production；保存创作成果不等于同意联网检索、采集来源、建立 research 记录或使用外部生成服务。关键方向、阶段定稿、Canon、生产批准等人工裁决必须由用户明确作出，不能自动代批。

## 新手引导与阶段成果落盘

- 默认采用**新手模式**，用户可随时说“切到协作模式”或“切到专业模式”；协作模式减少教学提示，专业模式可直接使用专业术语和结构化合同，但三种模式都遵守真实性、授权与人工裁决边界。每轮只推进一个可感知的小目标，最多提出一个真正阻断当前目标的问题；非阻断缺口用明确的可替换假设继续。首轮必须先产出一句核心、一个片段、一个方向判断或同等具体内容，不能只问问题。“不知道”是合法答案。
- 把“听起来不错”“推荐这个”“可以”“继续”“就这样聊”等视为反馈或继续探索，**不**视为阶段确认。只有用户明确说“阶段完成”“按此定稿”或同等无歧义表述，才记录人工阶段裁决；关键人工裁决不得由模型根据语气补写。
- 阶段内容已完整但尚未明确确认时，先给可独立阅读的完整正文，并把确认与保存合成至多一个问题，明确建议路径。用户已明确阶段完成且项目曾一次性授权“各阶段确认后保存”时，直接写新版本，不重复询问；没有该授权时，合并询问并主动提供“确认并保存为新的 Markdown 版本 / 确认但暂不保存 / 继续修改”三种自然选择。目录未知时并入同一个收尾问题。用户选择保存且目录明确后，自动从模板创建缺失的项目基础记录文件，不要求新手手工复制或填写内部字段。
- 总控阶段主文件使用 `项目简报_vNNN.md`；总控还负责创建或更新 `PROJECT_INDEX.md` 与 `SESSION_HANDOFF.md`。落盘必须保存可独立使用的完整正文，不能用聊天摘要、修改清单或占位符冒充。写前扫描同名模式并取最大三位版本号加一（首次 `v001`），绝不覆盖旧版；写后回读主文件，确认内容与路径，再更新索引中的当前版本/状态/链接以及交接中的本轮决定、未决项和下一步，并回读关键更新。任一步失败都报告实际失败与已完成范围，不得声称保存成功。
- 阶段确认、文件保存、研究同意、生产批准是彼此独立的授权。保存成功不自动使内容进入 Canon，不自动升级公共协议状态，也不授权 research、上传、付费、生成或发布；需要受管理交接时再按 `references/common-contract.md` 记录真实状态与证据。

## 1. 职责与启动

协调目标、阶段、依赖、能力、确认与最小回退，不替代角色/导演等专业判断。完整流程不是所有阶段都必须执行；用户已有资产只要一份提示词时，不重启创意开发。

先判断本轮工作深度，不把对话一律升级为项目管理：**讨论**直接回应当前问题、可保留未知；**探索草案**允许可替换假设与少量比较；**正式成稿**只完成用户指定范围；**生产交接/验证**或受管理产物上游换版才加载 `references/common-contract.md`，执行公共头、授权、真实证据和依赖复核。讨论/探索不强制完整公共头、候选状态、锁母版、生产测试、落盘或全流程；正式成稿输入足够时直接写，不用连续追问代替创作。

共同循环：当前意图→直接判断或局部试写→必要时少量替代与代价→吸收反馈→只改受影响项。保留已定、暂定、否决原因与开放项；一轮可停在未收敛的选择中，AI 预期不冒充真实观众反馈。

`related_skills` 是关联索引，不假设任何运行时会自动加载；确定阶段后显式读取目标 SKILL.md，按目标正文条件读取参考。无法加载时报告缺失阶段与可交付部分，不冒称执行完该技能。只追问会改变当前判断、创作结果或副作用边界的缺口。若多个方向均合理可提出推荐并保留假设，未经确认不变成 Canon。

## 2. 路由矩阵

|当前证据/请求|下一阶段|可跳过的工作|停止/回退条件|
|---|---|---|---|
|只有画面、观察、主题；还无可选择方向|ai-visual-ideation|剧本与生产|候选未选择不得当已批准方案|
|已有核心，要故事、场景、对白、动作谱|ai-screenplay-development|已确认核心不重做|核心因果/观看任务不清回创意|
|规则、空间、文化或材料机制影响作品|ai-worldbuilding|现实简单任务可只做约束卡|关键规则冲突等待裁决|
|要角色身份、结构、表演或系列资产|ai-character-design|可用最小brief，不必完整世界史|无角色项目跳过角色|
|要环境、光色、组合或空间母版|ai-scene-design|已有合法基准不重建|拓扑/支撑不成立先修空间|
|已有段落，要观看顺序、调度、连续性|ai-storyboard-design|单张资产无需镜头链|剧情事实改变先回剧本/世界|
|设计已清楚，要写静态或 Seedance/即梦正式提交文本|ai-prompt-execution-contract|不再重做整套设计|缺参考职责、任务合同或能力证据先补|
|输入已明确，要做 Clip/DO 分组、生成请求、续接或外部交接|ai-video-production-classroom|课堂限制仅选用时生效|无工具交自足执行包，不伪造视频|
|已有真实图/视频要做十维判断、尾帧资格或返修|ai-generation-review|只查相关参照，不重读全库|证据不足只评可观察项|

单纯问一个概念可直接解释，不强制建立项目记录。跨阶段任务按依赖推进；角色和场景在共享世界规则/风格确定后可独立探索，合成前检查比例、材质和行动空间。不得让一个分支私自改共享约束。

## 3. 三种合法路径

- narrative：核心→必要剧本/规则→角色与场景→分镜→合同→生成→评审。
- perceptual：感知目标→材料/空间约束→必要资产/观看谱→合同→生成→评审。character:null 合法，不补人物欲望。
- conceptual：命题与对照操作→材料与观察边界→必要资产/动作谱→合同→生成→评审。生成隐喻不能当科学证明。

路径可从任何已有有效输入进入。静态交付可在角色/场景/静态合同和图片评审结束，不强加声音、尾帧或视频。环境基准可以本来黑白、损坏、抽象、无人或有人。

## 4. 阶段闸门与协调

进入下游前核对：模式、唯一当前交付目标、公共头、依赖版本、所需批准范围、真实可用素材和完成判据。一个交付目标可以包含多个有功能的动作，不等于每镜只能一个动词。

- 未批准：可执行明确授权的探索/候选测试，输出仍candidate；不进入要求批准的正式生产。
- 平台未知：列需要核验的参考输入、时长、画幅、音轨、编辑能力，不用固定模型经验代替。
- 无工具：准备自足提交文本、计划素材、待核验参数和回传清单，等待外部结果。
- 真实结果：保留返回文件/ID及当时输入，再评审。无法观察结果时明确证据缺口。
- Seedance 路径：storyboard 的镜头先形成 production_group/Clip/DO，再形成独立 generation_request；生成后才建立 edit_unit。四层 ID 与时长账本分开，不能把“DO 已写”说成“视频已生成”。
- 平台证据：当前时长、素材槽位、输入模态、声音和容量必须按模型/入口/日期核验；官方事实、案例归纳、第三方经验和项目实测分层。
- 用户拒绝：保留理由，定位最小需改层，不默认重做全片。
- 上游变更：加载公共协议的影响传播步骤；先完成变更记录，再按依赖复核。不要自动覆写下游执行稿。

## 5. 最小回退

先区别“设计本身有问题”“正确设计未被正确提交”“提交正确但生成失败”“仅后期瑕疵”“证据不足”。身份、空间、动作错误是症状而非根因。输出竞争性假设和区分检查，优先保护已通过部分。

默认做可比较的小修改。耦合问题允许联合修复并声明归因限制；不得顺手优化未授权的摄影/时长/参考。规则和世界事实需要改变时先回上游提案，不能在Prompt中偷偷修Canon。

## 6. 输出与项目记录

所有可交接总控记录使用公共头；artifact_type 按当前被交接产物填写。下例是项目创意路由记录，尚未获得批准：

```yaml
artifact_id: IDEA-project-route
version: v1
artifact_type: idea
creative_mode: perceptual
subject_type: environment-led
lifecycle_status: draft
production_status: not_applicable
approval_level: none
upstream_versions: {}
confirmed_evidence: []
open_questions: []
recheck_trigger: []
next_action: develop_scene_brief
payload:
  character: null
  user_goal: 黑白废墟窗帘的光影变化
  current_stage: scene
  selected_skill: ai-scene-design
  skipped_stages: [character]
  reason: 无人物且空间与材质是当前待明确对象
  approval_gate: pending
  capability: 未提交生成
```

授权落盘后建立项目索引，链接产物ID/版本/状态，不要求固定目录树。阶段记录保留输入、AI提案、真实人工决定、否定理由、结果证据和修改影响。成熟项目可使用已有索引，不新建重复账本。文件不可写则给完整文本并标未落盘。

## 7. 可执行贯通例与自检

需要演示无角色链、外部生成等待或改版影响时，加载 `references/end-to-end-handoff.md`。示例是假想教学数据，不作为真实生成证据。需要核对来源与边界时加载 `references/source-map.md`。

- 当前阶段由用户交付目标决定，关联项已显式加载而非假定自动执行。
- 已有批准产物没有被无故重做；候选/计划/上传/返回/评审/人工确认分开。
- 模式与主体类型贯通，人物及声音等不适用字段未硬填。
- 基准符合项目，不自动恢复完好彩色。
- 依赖可追溯，改版的直接与传递影响已检查。
- 下一步最小动作、暂停原因或回退层可执行。

---

## ai-generation-review

---
name: ai-generation-review
description: "用于评审 AI 生成的角色、环境、物体、抽象图像、分镜帧及 Seedance/即梦视频；Use when judging identity, space, geometry, action, direction, props, camera, information boundaries, audio, tail-frame usability or repair priority. Separate observations from competing causes, design distinguishing tests, route post-fix/regeneration/storyboard return, and never infer motion or sound from stills."
version: 0.5.0
author: Domain Knowledge Distillation
license: MIT
metadata:
  hermes:
    tags: [review, evidence, quality-gate, continuity, ai-generation, seedance, diagnosis]
    related_skills: [ai-prompt-execution-contract, ai-video-production-classroom, ai-character-design, ai-scene-design, ai-storyboard-design]
---

# AI 生成结果评审｜证据、诊断与回归

## 新手逐轮引导与阶段成果落盘

任何交互先加载 `references/novice-guidance-protocol.md`；评审状态、授权、证据、人工裁决和依赖同时加载 `references/common-contract.md`。引导只降低交互负担，不删减证据边界、十维评审、竞争根因、回归和人工裁决。

阶段落盘必须保存完整正文，并在写后回读通过后更新 `PROJECT_INDEX.md` 与 `SESSION_HANDOFF.md`；这三个文件及其状态必须相互一致。

- 每轮只推进 **1 个小目标**，最多问 **1 个真正阻断当前目标的问题**；“不知道/你先看/都可以”合法。首轮即给小产出，例如按现有证据写一条“可判/不可判”观察，或列十维中最优先的一维及依据，不得只索取全套材料。Agent 补全必须标假设且可撤销。
- 用户否决假设时只撤销受影响判断；已有评审默认局部修改。跨会话先读项目索引、阶段交接、最新评审和所引证据；不可读时明确受阻，以用户最小摘要继续，不能伪造看过媒体或时间码。
- “看起来对/可以/继续”等积极反馈不等于阶段完成、保存授权、被评对象通过、人工裁决或研究同意。先交完整可独立阅读的评审正文。已有有效持续保存授权时，只问是否确认“评审报告阶段完成”，明确确认后直接保存；没有持续授权时，用一个合并问题主动提供“确认报告并保存为新的 Markdown 版本 / 确认但暂不保存 / 继续修改”三种自然选择，并说明预计文件名与已知目录。只有明确确认才把**报告自身**标 `confirmed`，被评对象仍按真实证据与人工裁决保持其状态。
- 确认与保存授权不得互相推导，但不得拆成两轮重复询问。目录未知时并入同一个收尾问题；用户选择保存且目录明确后，自动创建缺失的项目基础记录文件。保存授权不等于接受作品、重生成、上传、发布或研究授权。
- 主文件名为 `生成评审_对象_vNNN.md`，保存观察范围、证据位置/时间码、适用性、十维矩阵、症状、竞争根因、区分证据、修复/回归、人工裁决与未决项的**完整正文**，不能只存分数或摘要。证据不足保持 `unconfirmable`，人工未明确则 `pending`。
- 写前扫描版本，使用下一未占用三位号，旧评审不覆盖；写后回读核对，再更新项目索引和阶段交接。写入、回读、索引或交接失败只报未完成/部分完成。Skill、文件、图片、视频或音频工具不可用时交完整证据缺口评审/人工观察清单，不伪造观察、通过或人工裁决。
- 保存作品不等于同意研究、评测、训练、公开、上传、留存或二次使用；专项用途另取明确可撤销同意。

## 1. 边界与入口

评审对照任务与真实产物，不以“整体更漂亮”替代符合要求，也不把生成偶然性升为项目设定。分别报告观测、合规判断、根因假设和人工裁决。评审可以完成而人工决定仍 pending；代理通过不等于人工接受。

先判断工作深度：讨论时可针对一个疑点给出证据边界、竞争解释或局部评审草案，不强制完整公共头、全片检查或落盘；探索草案可比较修复责任层与最小试验，但不把候选结论升为通过；正式成稿只完整交付用户指定的评审范围。仅在生产交接/验证、明确要求登记受管理评审，或受管理上游换版时加载 `references/common-contract.md`，使用同一公共头、失效传播、写入和证据协议。授权落盘后保留旧稿。项目可改风格与验收偏好，不能授权虚构已看见内容或未发生的人工确认。

| 条件 | 加载文件 | 应得到的判断/输出 |
|---|---|---|
| 进入正式观察/验证、证据不完整或只有截图 | `references/evidence-and-scope.md` | 可判/不可判范围、取证步骤、缺证据出口 |
| 评审 Seedance/即梦视频、尾帧续接资格或视频返修 | `references/seedance-video-ten-dimension-review.md` | 十维观察、症状竞争解释、三路裁决和尾帧传播门 |
| 有失败症状、需要归因、修复或上游换版 | `references/routing-and-regression.md` | 竞争根因、区分证据/最小试验、责任层和回归 |
| 输出任何评审交接 | `references/review-template.md` | 公共头、适用性矩阵、人工 pending 与修复记录 |
| 需要环境/物体/抽象完成及失败示范 | `references/worked-examples.md` | 完成评审记录、失败诊断与有限证据边界 |
| 检查来源与继承缺陷修正 | `references/source-map.md` | 源版位置及证据协议增强说明 |

## 2. 判定词与证据协议

检查项状态为：

- `pass`：在声明范围内有证据支持满足要求。
- `fail`：可见/可听证据明确违背要求。
- `suspicious`：有具体疑点，但不足以判失败。
- `unconfirmable`：当前证据类型、质量或覆盖不足，不能判断。

不适用不是 pass：用独立 `applicable: false` 和理由，item_status 可为 null。environment-led/object-led/abstract 可 `character: null`，角色身份、表演与潜台词不强制评审；仍核对相关物体结构、空间/材料、变化范围与终点。narrative/perceptual/conceptual 用相应完成判据，不强塞 Narrative Job。

图片记录文件/版本、观察区域、可见事实和对照要求；不推断背面、遮挡后结构或生成过程。视频记录文件、完整观看/抽样区间、时间码、帧/区域、音轨检查方式。单张截图不能确认动作顺序、连续性、全片时长或声音；局部截图不能代表全片。文件存在不等于已观看，媒体元数据有音轨不等于已听过内容。

## 3. Seedance 视频十维入口

视频结果按适用性检查十维：**身份、空间、几何、动作、方向、道具、摄影、信息边界、声音、尾帧**。每维都要记录要求、证据位置/时间码、观察范围、状态和接口影响；不适用独立记录，不能当 pass。

- 身份/空间漂移是症状，不是根因；先查实际上传、素材冲突、景别/遮挡、动作与运镜负荷、原片/后期和随机性。
- 动作按起点→预备→启动→路径→接触→结果→Reaction 检查；后一镜重演已完成动作同样是失败信号。
- 刚性墙面、地面、桌面、门框和器具的液化/弯折属于几何维度，不能只看角色。
- 信息边界同时检查完整画幅边缘和音轨，未来人物、能力、地点或声音不得无授权提前出现。
- 尾帧必须连续观看邻近区间；身份、空间、几何、方向或关键道具错误时禁止作为下游参考。

裁决摘要：原片正确而后期引入问题→后期；输入/合同可修且导演意图不变→重生成；必须改变景别、机位、轴线、镜头数量、Reaction/Decision 或首次揭示→退回分镜。只有源资产自身矛盾才退资产层。单次随机失败不能证明责任层。

## 4. 评审闭环

1. **冻结参照**：读取本轮真实结果、实际 Prompt/素材/参数及必要上游版本。要求不明确时先作描述性观察，不能伪造合规标准。缺上传日志时标不能核对实际绑定。
2. **判定适用性**：按模式与主体类型选择身份、空间、结构/材质、动作、表演、摄影、声音和续接接口；静态不评运动，抽象不强制地面支撑/物理接触。
3. **记录症状事实**：例如“入口在该帧从左移至右”，先核对是否授权反打/反向视角，不能直接称空间拓扑坏了。用户指出某缺陷时围绕该缺陷取证，不偷换成较易观察的问题。
4. **逐项判定**：引用区域/时间码和要求；分清可见失败、可疑、无法确认。结果新构图可能有价值，但先登记候选。
5. **列竞争根因**：身份漂移可来自错误上传、参考冲突、遮挡/尺度、动作负荷、采样或模型限制；空间漂移可来自参考拓扑、机位表达、镜头边界、生成重建或后期镜像。不得直接路由角色母版或场景重做。
6. **区分证据/最小试验**：优先查原始文件、实际上传回执、提交快照、剪辑前后差异等无需新生成的证据；需要生成时提出可区分假设的对照，记录控制变量与预期结果。单次随机生成不能证实根因；重复比较也只支持当前条件，不宣称模型普遍规律。
7. **授权修复并回归**：基于证据选择最小负责层。默认一次控制一个主变量用于诊断；耦合变量可联合修复，但须明确为什么不能分开、联动范围、保护项与无法独立归因的限制。修复不是试验成功，必须重新观察结果及受影响接口。
8. **人工裁决**：无明确用户原话/审查记录则 `human_decision: pending`；接受、拒绝、局部吸收、退回均需引用证据。缺裁决不阻止交付评审报告，但不能晋级 human_confirmed 或覆盖批准资产。

## 5. 条件路由，不把症状当根因

| 区分证据支持的责任层 | 修复方向 | 回归范围 |
|---|---|---|
| 实际上传与目标版本不符，或职责语义互相覆盖 | Prompt/生产绑定 | 身份/材质及动作、空间是否被连带改坏 |
| 源资产自身在必要视角/结构已矛盾 | 角色/场景/物体资产 | 所有采用该版本且受影响的下游 |
| 镜头/动作合同缺路径、支撑或状态边界 | 分镜或提示词 | 原失败动作、前后状态及观看任务 |
| 提交正确，复杂请求反复失败，较简单对照改善 | 生产能力/分组候选 | 质量、接口连续性、重试成本；不能称已唯一归因 |
| 世界规则/状态逻辑自身冲突 | 世界/剧本；无叙事时回概念/材料规则 | 变化条件及后续结果 |
| 源片正确、后期引入镜像/切点/音量问题 | 后期 | 修改区间、相邻连接、最终音画 |
| 证据不足，假设仍竞争 | 暂不固定责任层 | 请求素材/日志或最小试验，不盲目重做上游 |

## 6. 输出合同

详细模板见 `references/review-template.md`；下列公共头与核心 payload 同样可直接交接。制品 version 与技能 0.5.0 分开。

```yaml
artifact_id: REVIEW-example
version: v1
artifact_type: review
creative_mode: perceptual
subject_type: environment-led
lifecycle_status: draft
production_status: not_applicable
approval_level: none
upstream_versions: {}
confirmed_evidence: []
open_questions: []
recheck_trigger: []
next_action: request_evidence
payload:
  character: null
  result_artifact: null
  actual_input_record: null
  requirement_refs: []
  observation_scope: {visual: null, intervals: [], audio: not_checked, method: null}
  facts_observed: []
  review_matrix: []  # applicable、requirement、evidence、scope、item_status
  symptoms: []
  competing_root_causes: []
  distinguishing_evidence: []
  minimal_tests: []
  repair: {responsible_layer: undetermined, allowed_changes: [], coupled_rationale: null}
  regression: {protected_scope: [], affected_checks: [], adjacent_interfaces: []}
  rollback_path: null
  human_decision: pending
  human_decision_evidence: null
  proposed_upstream_changes: []
```

`production_status: not_applicable` 属于评审文档自身；受评 clip 的状态在其生产记录更新为 under_review/reviewed，不把 review 文档当生成视频。completed 不是新增公共状态。

## 7. 受阻分支与上游改版

- 无视觉/视频/音频观察工具：可核对文本合同和文件元数据，但观察范围如实写；需实际观看项 unconfirmable，交人工观察清单，不能装作看过。
- 缺结果文件或只有失效链接：请求真实媒体；可完成“证据缺口评审”，不得填图像区域/时间码事实。
- 生成提交失败：评审实际失败日志和合同，不把不存在的画面判 fail；生产记录 failed，视觉判 unconfirmable。
- 用户拒绝补素材/重生成：保留已观察结论和拒绝依据，不擅自调用；未判项保持 unconfirmable。
- 人工等待：pending 合法，next_action 指向 await_human_review；拒绝是否为影片质量拒绝、补素材拒绝或执行拒绝应分别记录，不能相互替代。
- 上游换版：对直接及传递受影响依赖标 needs_recheck，保留旧批准历史但取消其当前门禁效力；例如场景拓扑改变使 Prompt、Clip、Review 的空间结论待复核。无关项经影响检查可保留理由，不一刀切全包失效；完成重审才恢复 confirmed。

## 8. 质量闸门

- [ ] 必要参照版本、真实输入与观察能力明确，character:null 和非叙事完成判据可贯通。
- [ ] Seedance 视频十维按适用性逐项记录；方向、道具、信息边界与尾帧没有被并入泛泛“连续性”。
- [ ] 坏尾帧已标不可复用；文字重启没有冒充真实尾帧续接。
- [ ] 后期、重生成、退回分镜与退回资产层有证据门，不凭单次随机失败跳层。
- [ ] 事实与解释分开，证据位置/时间码和观察范围完整。
- [ ] 没用静帧证明动作、连续性或声音，没有用元数据代听觉检查。
- [ ] pass/fail/suspicious/unconfirmable 与不适用分开。
- [ ] 身份/空间仅作为症状，竞争根因及区分办法已列；单次随机生成未被当因果证明。
- [ ] 修复范围受控；必要的联合修复有耦合理由和归因限制。
- [ ] 回归覆盖受影响项与前后接口，旧结果和回退路径保留。
- [ ] 人工裁决真实或明确 pending；局部通过未扩大为全片通过。

---

## ai-prompt-execution-contract

---
name: ai-prompt-execution-contract
description: "用于把已确认的创意、角色、环境、物体、抽象视觉或分镜转成可执行且可评审的生成合同；Seedance/即梦正式 DO、逐镜视频提示词、参考素材职责、状态接力和容量压缩也触发。Use when writing, auditing, assembling or repairing static-image and temporal-video prompts, including self-contained Seedance generation requests. Separate record shorthand from submitted payload, encode observable outcomes, and preserve authorization and evidence boundaries."
version: 0.5.0
author: Domain Knowledge Distillation
license: MIT
metadata:
  hermes:
    tags: [prompt-contract, prompt-engineering, ai-image, ai-video, seedance, references, continuity]
    related_skills: [ai-storyboard-design, ai-character-design, ai-scene-design, ai-video-production-classroom, ai-generation-review]
---

# AI 影像提示词执行合同

## 新手逐轮引导与阶段成果落盘

任何交互先加载 `references/novice-guidance-protocol.md`；状态、授权、证据、提交与依赖处理同时加载 `references/common-contract.md`。引导不得弱化自包含合同、参考职责、平台能力核验、状态接力和真实性边界。

阶段落盘必须保存完整正文，并在写后回读通过后更新 `PROJECT_INDEX.md` 与 `SESSION_HANDOFF.md`；这三个文件及其状态必须相互一致。

- 每轮只推进 **1 个小目标**，最多问 **1 个真正阻断当前目标的问题**；“不知道/你决定/都行”合法，给标明假设且可撤销的默认。首轮交一个小产出，例如目标—约束—可观察结果—完成判据四行骨架或一个可复制请求片段，不只索取参数。
- 用户可否决任何 Agent 假设，只撤销受影响内容；已有合同默认做局部修改。跨会话先读项目索引、阶段交接和最新版本；不可用则明确缺口，以用户最小摘要重建，不伪造上游、素材或平台状态。
- “好/可以/继续”等不等于阶段完成、保存授权、提交授权或研究同意。先交完整可独立执行正文。已有有效持续保存授权时，只问是否确认本阶段完成，明确确认后直接保存；没有持续授权时，用一个合并问题主动提供“确认并保存为新的 Markdown 版本 / 确认但暂不保存 / 继续修改”三种自然选择，并说明预计文件名与已知目录。只有明确确认才标 `confirmed`。提示词合同保存/confirmed 仍不代表已上传、submitted、generated 或 production_approved。
- 阶段确认与保存授权不得互相推导，但不得拆成两轮重复询问。目录未知时并入同一个收尾问题；用户选择保存且目录明确后，自动创建缺失的项目基础记录文件。上传、付费、提交、生成、发布和研究始终分别授权。
- 主文件名为 `生成请求_对象_vNNN.md`，保存记录层、完整实际提交层/可复制请求、素材职责、参数待核、状态、完成判据、未决项与回传清单的**完整正文**，不能只存摘要。无生成工具也要交付完整请求并如实标 `awaiting_external_generation`/待外部执行，绝不伪造上传、任务 ID、返回文件或生成结果。
- 写前扫描版本，取下一未占用三位号且不覆盖旧版；写后回读核对存在、版本和请求正文，再更新项目索引与阶段交接。任何写入、回读、索引或交接失败都只报未完成/部分完成和恢复步骤。文件、Skill、素材/媒体不可用时输出完整可复制 Markdown，不声称已读取、写入或观察。
- 保存作品不等于研究、评测、训练、公开、上传、留存或二次使用同意；专项用途另取明确可撤销同意。

## 1. 边界、进入条件与参考路由

本阶段把创作判断变成一次请求可独立执行、结果可观察的合同，不代替导演设计，也不把提示词完成等同生成成功。静态图可从已明确的资产/构图需求直接进入，不必虚构剧本或分镜。

先判断工作深度：讨论时直接回答或局部试写，不强制完整公共头、提交层或生产测试；探索草案可比较可替换的合同假设，但标暂定、不升为生产 candidate；正式成稿只交付用户指定范围。仅在生产交接/验证、明确要求登记受管理产物，或受管理上游换版时加载 `references/common-contract.md`，此时公共状态、版本失效传播、证据及文件写入以该协议为准。授权落盘后确定工作区、保留版本，不静默覆盖批准稿。正文是通用默认，参考是条件方法；明确授权的项目格式可以覆盖格式偏好，不能覆盖证据真实性与授权。

| 条件 | 加载文件 | 取得的决策/产出 |
|---|---|---|
| 第一次使用或复合任务不知先后 | `references/methodology-map.md` | 分支顺序与交接检查 |
| 静态/视频/非叙事字段选择 | `references/static-vs-temporal.md` | 必需与不适用字段、完成判据 |
| 有参考图、继承上下文或需压缩提交 | `references/reference-duty-and-assembly.md` | 事实→读取范围→禁止继承、提交账本 |
| 动作、抽象效果、失败诊断或局部修复 | `references/causality-and-diagnostics.md` | 正向生命周期、竞争假设、修复范围 |
| 要写可复制的 Seedance/即梦正式 DO 或逐镜视频稿 | `references/seedance-formal-request-contract.md` | 自包含请求块、逐镜五字段、确定性结构校验 |
| 相邻请求需要真实尾帧、文字状态或独立重启 | `references/seedance-state-handoff.md` | 接口来源、状态字段、禁止重演与坏尾帧阻断 |
| 当前入口有已核验容量限制且提交块超限 | `references/seedance-capacity-compression.md` | 详细审核版→精简提交版的无损压缩与回归 |
| 需要完整交付或异常分支示范 | `references/worked-examples.md` | 完成合同、缺证据与失败记录 |
| 核查方法来源/通用化边界 | `references/source-map.md` | 已读源版具体位置与保留/改写理由 |

## 2. 先选择模式，不强加人物叙事

携带 `creative_mode: narrative | perceptual | conceptual` 和 `subject_type: character-led | environment-led | object-led | abstract`。二者独立：物体可承担叙事，角色也可仅作感知观察。

- character-led：引用必要角色版本，核对可见身份、姿态/比例。人物弧线不是每次生成的输入。
- environment-led：`character: null` 合法；以空间、材料、光色、环境变化和可读锚点验收。
- object-led：`character: null` 合法；以结构、接触/支撑、材质及状态变化验收。
- abstract：`character: null` 合法；以覆盖范围、材质、形态、运动路径和终点验收，不凭空增加角色、场景拓扑或物理接触。

通用必需是 **目标—约束—可观察结果—完成判据**。`narrative_job` 只在叙事信息/情绪职责相关时选填；非叙事填感知目标或概念关系，静态图不强塞 Narrative Job。

## 3. 区分三种单位和两层输入

- **镜头 shot**：导演规定的取景/观看/切点单位。
- **剪辑单元 edit_unit**：后期选用、裁切、排列的一段素材，可以包含多个镜头或一个镜头的部分。
- **生成请求 generation_request**：平台实际一次调用；可覆盖多个镜头，也可仅生成一个镜头的部分。一次请求的返回可以裁成多个剪辑单元，同一剪辑单元也可能来自多次补生成。

不要让同一个 Clip/DO 名称暗中承担三种含义。分别记录稳定 ID、覆盖关系、导演目标时长、请求窗口及计划/实际裁切；时长策略交 `ai-video-production-classroom` 的能力矩阵，不固定长短、秒数或每镜时长。

记录层可用“项目常量 + 本镜差异 + 上游版本”；提交层必须组装完整必要上下文、实际可用素材与参数。平台不自动读取本地路径或前次对话。若确有上下文继承，记录服务支持及实际会话/资源证据，不把计划视为上传。

## 4. 执行步骤

1. **冻结要求**：确认授权为写稿、上传、付费提交还是仅审查；锁定任务、主体类型、必要上游稳定 ID→版本、完成判据和不可改范围。上游候选可用于探索，但标候选、不能冒充生产批准。
2. **选择合同**：`static-image` 或 `temporal-video`；局部修复是 `operation: local-repair`，仍保留媒体类型。只保留适用字段。
3. **核验素材**：区分文件存在、视觉已看见、实际已上传三个事实。逐份记录可见事实→本镜读取范围→禁止继承。整图可以用于全局拓扑或经确认的全貌构图；禁止的是无职责的笼统继承，不是一律禁全图。多面板按实际文件计上传数，面板定位另记。
4. **组装正向合同**：静态写主体/结构/状态、构图焦点、材质与判据；时序按起始→触发→路径/接触（适用时）→反馈→终点。不把所有变化压成一个形容词，也不要求抽象色场有手、地面或反应镜。
5. **摄影和声音**：运镜写初始机位、触发、运动路径、停稳/切点；固定镜头直接写固定。音轨仅在任务需要时写，未知生成能力留问项，不保证模型产出。普通结束状态融入镜头结果，只有真实续接或关键末帧职责才单列接口。
6. **校对提交层**：反向核对每个必需约束在文本/参数/素材的哪个位置落地；风格、全局状态也必须在实际请求中可见。压缩只去重复，不删身份、状态范围、方向、因果和可见终点。核对字符/槽位等当前限制，未知时停在待核验。
7. **交付**：合同本身完成可交付，真实生成结果另由生产记录跟踪。提交前保留授权和输入快照；返回后转 `ai-generation-review`，不提前宣称合格。

### Seedance 正式执行稿分支

当用户只要求拆分 Clip/DO 时，先交生产组与请求映射；当用户明确要求“正式 DO”“可复制 Seedance 提示词”时，才加载正式请求合同并逐请求展开。`DO` 是可选显示别名，不得暗中混同导演镜头、平台调用和剪辑采用段。

每个独立平台请求必须自包含：请求窗口与画幅、视觉状态、唯一任务及完成判据、主体边界、实际上传素材职责、逐镜镜头任务/构图动作/摄影注意力/声音切点，以及真实需要时的续接接口。公共 YAML、内部路径和审批记录留在记录层，不粘入平台代码块。

Seedance 的时长、素材数量、输入模态和字符限制是动态能力。只采用带官方来源、核验日期、模型、产品入口和模式的当前证据；官方案例归纳、第三方经验与项目实测分别标注，均不冒充永久官方语法。

## 5. 公共头与输出模板

以下是记录模板，不把行政字段直接粘入平台提示词。`version` 是该制品修订号，技能版本为 0.5.0。引用素材的 ID 不含版本。空列表表示尚无证据，不代表已核验。

```yaml
artifact_id: PROMPT-example
version: v1
artifact_type: prompt
creative_mode: perceptual
subject_type: environment-led
lifecycle_status: draft
production_status: not_applicable
approval_level: none
upstream_versions: {}
confirmed_evidence: []
open_questions: []
recheck_trigger: []
next_action: request_review
payload:
  contract_type: temporal-video
  operation: create
  character: null
  target_goal: null
  narrative_job: null  # 仅叙事相关时填写
  completion_criteria: []
  record_layer: {project_constants: [], shot_delta: [], protected_scope: []}
  units: {shot_ids: [], edit_unit_ids: [], request_id: null}
  capability_evidence: []
  submission_layer:
    platform: null
    model: null
    parameters: {}
    complete_required_context: null
    positive_prompt: null
    actual_uploaded_assets: []
    planned_but_not_uploaded: []
    verified_context_inheritance: []
  reference_duties: []  # asset_id/version/file、可见事实、读取范围、禁止继承、上传证据
  static_fields: null  # 静态时填构图/焦点/结构/状态/材质，删除 temporal_fields
  temporal_fields: {state_chain: [], camera: null, audio_requirement: null}
  next_segment_interface: null  # 仅实际续接职责时填写
  allowed_changes: []
  coupled_repair_rationale: null
  high_risk_constraints: []
  blocked_reason: null
  human_decision: pending
  human_decision_evidence: null
```

## 6. 能力缺口与失败出口

- 无工具：交付独立可复制文本、计划素材、参数待核项和外部回传清单；待执行的 clip/request 用 `awaiting_external_generation`，提示词制品本身保持 `not_applicable`。不得把计划清单标为已上传。
- 缺素材/无法看图：列缺失项和受阻判据；可给候选文本，不编视觉事实。用户明确授权改纯文字方案时另存候选并注明不再保证图像身份/拓扑继承。
- 提交失败：生产记录为 `failed`，记录错误及已知任务句柄；未确认受理的超时先查询，不能盲目重复付费调用。不得虚构结果文件。
- 用户拒绝生成/预算：保留合同，记录拒绝依据，`next_action: await_user_authorization`，不提交。
- 人工等待：`human_decision: pending`，不代填接受；`next_action: await_human_review`。有明确原话/裁决引用才更新决定和批准层。

## 7. 诊断、版本与闸门

失败处理采用 **症状→竞争根因→区分证据/最小试验→授权修复→回归**。身份/空间漂移不是根因；先比较实际输入与合同，再考虑资产、提示词、镜头负荷、采样及平台能力。单次随机生成不能证实根因。默认控制一个主变量用于归因；耦合变量可联合修复，但须列明共同原因、必要联动、保护范围，不能声称已隔离每个变量贡献。

上游改版时按稳定 ID 追踪直接及传递受影响依赖→`needs_recheck`，保留旧批准历史但不继续作为当前门禁；无关依赖经影响检查可保留并记理由。重审后才恢复 `confirmed`。

- [ ] 模式贯通，`character: null` 未被当缺角色错误；静态/时序字段适用。
- [ ] 镜头、剪辑单元和生成请求覆盖关系明确，没有固定时长假设。
- [ ] 参考事实、读取范围、禁止继承、计划/真实上传分开。
- [ ] 正向结果可观察；抽象效果的范围、材质、路径、终点可检查。
- [ ] 提交层完整，不依赖平台不可见文档；未知能力和缺素材未掩盖。
- [ ] Seedance 正式稿一个请求一个闭合代码块；镜头 ID、五字段、时长、实际素材与续接接口确定性闭合。
- [ ] DO 仅作显示别名；shot、production_group、generation_request、edit_unit 没有混号。
- [ ] 平台固定数值有当前来源/日期/入口；工程启发式未冒充官方规则。
- [ ] 根因与症状分开；修复有授权、保护范围及回归项。
- [ ] 没有虚构返回、通过或人工裁决；等待和失败均有下一动作。

---

## ai-scene-design

---
name: ai-scene-design
description: "Use when discussing or designing narrative and non-narrative environments for AI films or animation. 支持场景讨论、探索草案及非叙事感知/概念设计，推导运行空间、构图关系、光色信息显隐；按需深化拓扑、尺度、母版派生与生产交接，不替代具体平台操作手册。"
version: 0.5.0
author: Domain Knowledge Distillation
license: MIT
metadata:
  hermes:
    tags: [scene-design, environment, spatial-topology, composition, lighting, ai-animation]
    related_skills: [ai-worldbuilding, ai-storyboard-design, ai-prompt-execution-contract]
---

# AI 影像场景设计｜可执行封装

## 新手逐轮引导与阶段成果落盘

任何交互先加载 `references/novice-guidance-protocol.md`；涉及状态、授权、证据、依赖或交接时同时加载 `references/common-contract.md`，二者冲突时以真实性和用户授权为最高优先。新手协议只改变对话节奏与落盘门禁，不删减本技能的拓扑、构图、光色、母版派生和生产闸门。

阶段落盘必须保存完整正文，并在写后回读通过后更新 `PROJECT_INDEX.md` 与 `SESSION_HANDOFF.md`；这三个文件及其状态必须相互一致。

- 每轮只推进 **1 个小目标**，给用户可直接选、改、否决的具体内容；最多提出 **1 个真正阻断当前小目标的问题**。“不知道/你先建议/都可以”是合法输入，立即给带假设标签的可撤销默认方案，不以问卷阻塞。首轮即交一个小产出，例如三句场景功能、文字拓扑或两个带代价的构图方向，而不是只收集信息。
- 用户可随时否决 Agent 假设；只撤回受影响项并保留原材料与已确认内容。已有材料默认局部修改，不借新手流程全量重写。跨会话先读取项目索引、阶段交接和最新版本恢复“已定/暂定/否决/未决”，不可读取时明确受阻并用用户提供的最小摘要继续。
- “不错/可以/继续/就是这个方向”等积极反馈只表示本轮方向反馈，**不等于阶段完成、保存授权或任何研究同意**。先交付当前阶段可独立阅读的完整正文。已有有效持续保存授权时，只问是否确认本阶段完成，明确确认后直接保存；没有持续授权时，用一个合并问题主动提供“确认并保存为新的 Markdown 版本 / 确认但暂不保存 / 继续修改”三种自然选择，并说明预计文件名与已知目录。只有明确确认才可标 `confirmed`；仍有阻断缺口则保持 `draft/candidate`。
- 阶段确认与写文件仍是两道门，但不得拆成两轮重复询问。目录未知时并入同一个收尾问题；用户选择保存且目录明确后，自动创建缺失的项目基础记录文件，再执行完整落盘。持续授权可跨轮沿用，用户可随时收窄或撤销，不外推到上传、付费、生成、发布、研究、评测、训练或数据留存。
- 本阶段主文件使用 `场景设计_名称_vNNN.md`（名称取已确认场景名，安全化空字符）；保存**完整正文**及必要公共头、已定/暂定/未决、依据和下游约束，不能只存摘要、聊天记录或差异说明。先扫描版本，使用下一个未占用三位号；旧版和已确认稿一律不覆盖。
- 写入后必须回读，核对文件存在、标题/版本和正文关键段完整；再更新项目索引与阶段交接中的产物路径、版本、状态、上游版本、未决项和下一动作。任一步失败都如实报告“未完成/部分完成”及失败点，不得称保存成功。文件、Skill 或媒体不可用时交付可复制的完整 Markdown 与恢复清单，不伪造写入或观察。
- 保存作品只表示用户授权把该作品写入项目目录，**绝不等于同意研究、评测、训练、公开、上传或二次使用**；这些用途必须另行取得明确、可撤销的专项同意并记录范围。

## 1. 定位与使用边界

本技能服务叙事、感知、概念型 AI 短片、短动画和教学：把“一个地点/一场戏/一个镜头”从创作意图转为**可进入、可观看、可连续、可生产**的环境方案。场景不是漂亮背景，而是主体变化、观众理解和世界规则共同发生的空间。

先按本轮意图选择：**讨论**直接答问题；**探索草案**允许暂定假设和联动候选；**正式成稿**只完整交付用户指定范围；**生产交接/验证**才加载 `references/common-contract.md` 与 `references/execution-template.md` 的完整公共头、证据和生产闸门。受管理产物上游改版也加载公共协议。讨论不要求母版、动作测试、完整 YAML、落盘或生产包；探索候选不等于正式可交接 candidate。character: null 合法；无角色作品以材料/空间约束、可观察变化和完成判据替代人物目标/阻力，不添加人物过闸门。

讨论循环：当前意图 → 直接判断或具体试写 → 必要时比较替代及代价 → 吸收反馈 → 只改受影响项。可保留未知与不收敛；内部记住已定、暂定、否决原因和开放项，不强制每轮列表。输入足以成稿时直接写，只追问会改变当前判断的缺口。

核心判断：制作空间成立不等于观众时刻知道全貌；平面、有限、暧昧空间都是可选表达。构图先比较“压力落在谁或什么上”，而非自动增加深度；光色同时决定信息何时出现。先分清缺灵感还是缺证据：探索允许联动方案，故障诊断优先单变量，批准的共享约束不静默改写。

完整生产设计可参考以下顺序；讨论和局部成稿按问题进入，不强制走完：

> 叙事功能 → 空间拓扑 → 支撑面与尺度 → 摄影机/画幅/动线 → 形式与明度 → 光色/材质/天气 → 世界观证据 → 基准图与派生状态 → 分镜/3D交接。

本技能不包含项目 Canon、个人审美偏好、某平台参数配方或平台 SOP。若需图像生成，只提供可验证的设计约束与提示词结构；平台调用服从包内相应技能或用户指定工具。

按任务条件加载：
- 构图/光影/透视诊断时读取 `references/core-methodology.md`，得到症状—修复—回归表。
- 生产交接记录时读取 `references/execution-template.md`，填公共头与适用专业字段。
- 既有母版派生、上游改版时读取 `references/state-derivation.md`，确定差量和复核传播。
- 缺灵感还是缺证据、该先验证什么时读取 `references/production-handoff.md` 的低成本入口；生产结构风险涉及动作时再做中性组合与占位测试。
- 前后台运行、构图关系语气、平面/有限/暧昧空间或联动探索时读取 `references/spatial-intent-and-exploration.md`。
- 整片配色不连贯、风格/材质/光色混淆时读取 `references/color-script.md`，制作序列颜色路线。
- 追溯方法来源或审查迁移边界时读取 `references/source-map.md`。

## 2. 触发条件与初始诊断

当用户要求设计环境概念、场景母版、局部近景、镜头空间、Light Key、材质方案、世界观地点、连续场景或场景资产清单时使用。

先判断任务类型，不要直接写长提示词：

| 类型 | 首要问题 | 首个交付 |
|---|---|---|
| 新场景 | 这个段落为何存在，主体如何行动或材料/空间如何被观看？ | 场景 brief + 拓扑草案 |
| 已有母版派生 | 哪些内容必须冻结？ | 基准图差异表 + 单变量变更 |
| 只改光色/材质 | 是否允许改结构？ | 参考职责表 + 光色/材质模块 |
| 镜头局部 | 观众必须看懂哪个动作/信息？ | 局部机位与可读性方案 |
| 连续地点 | 上一空间如何进入下一空间？ | 拓扑连续表 + 母版序列 |
| 3D/分镜交接 | 哪些约束会影响 layout/资产？ | 交接包与未决项 |

输入缺失时可做 **V0**，但必须把假设标成“待确认”，不能把猜测伪装为 Canon。若已有人工通过的母版，母版优先于一般理论；除非用户明确要求重设计。

## 3. 按范围选用的八步方法（不是每轮必走流程）

### Step 1｜提炼场景功能与信息优先级

写出三句话：
1. **功能**：建立地点/展示规则/支持行动/制造冲突/揭示信息/完成情绪转折？
2. **第一眼**：观众先看到什么？必须由一个主焦点承担。
3. **第二眼**：观众随后理解什么关系、危险、目标或世界证据？

同时列出不可丢失信息：主体位置与朝向、目标物、入口/出口、危险边界、时空状态、必须保留的负空间。没有明确功能时，先按“建立地点身份 + 支持已声明的观察/行动”提候选，不默认添加角色。

### Step 2｜建立世界观因果，而非装饰清单

用一句 premise 说明地点是什么、靠什么运转、与现实哪里不同。再回答：谁在这里生活/工作/消费？资源从哪里来？交通、能源、排水、储藏、维护如何发生？权力、信仰、气候和历史如何改变建筑、道具与材料？

每个独特造型至少绑定一个因果：资源、功能、制度、信仰、气候、冲突或生活习惯。幻想也要有现实功能锚点；“奇幻、未来、东方、废土”只是标签，不是设计证据。环境叙事用新旧、修补、磨损、临时结构、路径痕迹、阶层差异表达，并控制在次要信息层，不抢主叙事。

### Step 3｜先画空间拓扑，再谈画面风格

建立俯视或文字拓扑，至少标注：
- 边界：墙、崖、林缘、水面、天空开口、不可进入区；
- 节点：入口、出口、核心地标、交互点、冲突点、遮挡点；
- 连接：道路、台阶、桥、门、视线通道、角色行动路径；
- 支撑面：脚下站立面、座面、操作台、平台、悬挂/漂浮的受力或叙事依据；
- 层级：前/中/远景，遮挡顺序，地面—建筑—远景的相对尺度。

具象空间回答“主体受何支撑/约束、如何移动或变化、接触什么、摄影机从哪里看”；纯抽象构成按已声明形式规则检查，不硬造地面或物理机制。悬浮、断裂、无重力不是免除支撑逻辑；要说明支撑机制或明确其为异常状态。宏观地点未锁定时，先比较整体地理、行动路径、核心空间三层候选，再进入精修。

### Step 4｜确定摄影机、画幅与视觉动线

先定眼平线/相机高度、观察方向、透视类型和镜头任务，再选画幅。画幅是叙事容器，不是固定配方；常见 16:9、1.85:1、2.35:1 或竖幅都必须由信息分布决定。转换画幅时先保护角色—目标关系、方向、入口出口和主焦点，采用非对称裁切并复查动线。

构图采用：主导流向 → 转折点 → 反应/释放空间。道路、河流、光束、建筑边线既可以是视线轨道也可以是行动轨道。用前景框景与遮挡建立尺度和深度，但不要堵住关键动作。用重复建立秩序、特异制造焦点、渐变制造流动、发射强化聚焦；主次靠尺寸、剪影、明度、对比、细节密度和虚实控制。

需验证可读性时选缩略图（二值大关系）、静音单帧（当前应知方向/目标）、镜头裁切等最低成本测试。讨论可只比较方案；未观察图像不声称测试通过。有意隐藏部分关系时检查揭示时机，不要求全时清楚。

### Step 5｜形式语言与空间可读性

先定大轮廓和剪影，再定细节。硬边、尖角、直线倾向力量/人工/危险；圆角、软曲线、团块倾向有机/亲和/包覆；不要让互相冲突的形体语言无理由混在一起。检查点线面节奏、正空间实体框架、负空间呼吸与穿插；远景靠轮廓与大色块，近景 Hero 才承担高密度材质。

透视检查：眼平线是否一致？同组平行线是否指向一致消失点？圆/椭圆短轴是否符合平面法线？建筑、角色和道具是否共享尺度参照？必要时输出平面图、正交/立面参考或简化盒体 blockout，而不是用形容词掩盖结构缺失。

### Step 6｜Light Key、明度、色彩、材质与天气

生产精修且存在结构/动作风险时，按 `references/production-handoff.md` 做中性组合，验证扫掠空间、接触与机位；无角色项目只检查适用运动包络。纯光色讨论不先要求动作测试。结构失败先回布局，不用风格覆盖。序列光色及边缘、反射、水体的信息显隐按 `references/color-script.md` 处理，不将单张配色自动推广全片。

先用两到三块明度组织体积和焦点，再叠加色彩与纹理。定义主光方向、光源面积/硬软、范围与衰减、天空/环境补光、反射光、投影方向和边缘质量。阴影不是加黑：固有色受环境光与邻近反射影响；亮面、暗面、核心阴影、反射光和投影必须互相解释。硬光配清晰投影，软光配扩散边缘；多光源要检查每个投影和补光来源。

色彩只锁关系，不无条件锁死具体色值：有限色域可统一镜头组；高彩度通常集中在焦点或关键物。正常晴天、阴天、黄昏、夜景、雾雨等是条件分支，不是硬规则：依据时间、天气、叙事情绪和可读性选择。前中远景通常逐步降低对比、饱和和细节，但逆光尘雾可产生反向大气透视。

材质至少写反射/粗糙/透射特征、纹理尺度、受力与风化原因。金属依赖环境反射，玻璃同时反射/折射且边缘更显著，布料偏哑光，水面受视角、风和天空影响。污垢聚在凹处、流痕服从重力、边缘磨损来自接触；所有风化都要有原因。

### Step 7｜建立基准状态与剧情派生

先命名一个“场景基准状态”，经授权再保存：空间完整性、地标位置、支撑面、相机方向、画幅、Light Key、材质基线和风格媒介。直接采用项目确认的基准，可能是废墟、夜景、异常或角色在场；不默认要求正常、完整、彩色、无人。为隔离拓扑可暂用无人测试图，但它不反向更改项目基准。

正式派生采用差异表：保持项 / 改变项 / 新增项 / 删除项 / 不得提前出现。故障诊断优先只改一个问题簇以便归因；新方向探索可联动构图、光色、遮挡，并记录各方案整组差量，不把整组胜出归因于单一因素。授权确认后才冻结版本。稳定 ID 与 version 分离，派生记录 upstream_versions，不覆盖母版；文件可回退不等于剧情状态必须可逆。局部修正仍导致拓扑漂移，回退到最近通过版本。

### Step 8｜输出交接包并做闸门

仅在用户要求完整生产交接包时，交付场景 brief、拓扑/布局、构图与机位、Light Key、色彩/材质、环境证据、版本表、资产分级、分镜约束与 3D 未决项。局部成稿只交指定部分；探索提示词可带明确假设，不伪装生产就绪。生产提示词需已有对应执行约束；参考图逐项注明职责（结构、比例、材质、光色、笔触等），不能越权改写空间。

## 4. 可直接复制的最小输出模板

```yaml
artifact_id: SCN-example
version: v1
artifact_type: scene
creative_mode: narrative
subject_type: character-led
lifecycle_status: draft
production_status: not_applicable
approval_level: none
upstream_versions: {}
confirmed_evidence: []
open_questions: []
recheck_trigger: []
next_action: request_review
scene_id: S__
character: null
scene_function: ""
first_read: ""
second_read: ""
world_premise: ""
world_evidence: []
space:
  boundary: []
  landmarks: []
  entry_exit: {entry: "", exit: ""}
  action_path: {start: "", goal: "", obstacles: [], interactions: []}
  support_surfaces: []
  scale_reference: ""
  topology_notes: ""
camera:
  shot_task: ""
  position_height_direction: ""
  perspective: ""
  aspect_ratio: ""
  foreground_midground_background: ["", "", ""]
  visual_flow_turn_release: ""
light_key:
  key: "方向/面积/硬软/范围/衰减"
  fill_and_reflection: ""
  shadow: "方向/颜色/边缘"
  atmosphere: "条件与密度"
look:
  form_language: ""
  value_structure: ""
  color_relation: ""
  materials: []
state:
  baseline: ""
  variant_delta: "保持/改变/新增/删除"
assets: {hero: [], supporting: [], fill: [], dynamic: []}
handoff:
  storyboard_constraints: []
  layout_3d_constraints: []
  unresolved: []
quality_gate: {result: pending, reasons: []}
```

## 5. 参考图、生成与教学分支

- **仅做空间母版**：可移除非必要人物和小道具以隔离结构，但保留任务必需主体/尺度参照；动作验证时另放中性占位，不改项目基准。
- **局部镜头**：从已通过母版的相机方向推进；母版锁空间，局部裁切只锁区域，第一轮先保留表演负空间。
- **外部参考图**：先声明参考职责；故障诊断优先单变量吸收，探索可比较授权范围内的联动方案；色彩参考不得擅自重排已锁地标。
- **连续场景**：生产序列逐张验收入口、拓扑、方向、信息边界与光色；未通过不作下游有效依据。探索可以并排比较整条序列，不必逐张批准后才画下一张。
- **状态图**：先锁基准，再派生正常/危机/恢复、晴/雨/夜、完整/受损等状态；状态之间共享空间身份但不强行共享不适用的光色。
- **教学输出**：解释“为何这样判断”和“如何验证”，优先给表格、字段、闸门和一个最小示例，避免平台术语堆砌。

## 6. 常见失败与修复

1. 只有氛围没有拓扑：补入口、出口、节点、支撑面和行动路径。
2. 基准与差量混写：按项目确认状态拆 baseline 与 variant，不预设基准必须正常。
3. 所有颜色、材质、细节同等强：回到二值明度、主次和远近细节梯度。
4. 世界观靠装饰：为每个独特点补资源/功能/制度/行为原因。
5. 角色无法站立或交互：补支撑面、尺度参照、操作高度和受力/魔法机制。
6. 参考图重排空间：拆分参考职责，冻结不可变项，单变量返修。
7. 每个镜头重新发明地点：继承母版，记录摄影机移动与物理地标不变的区别。
8. 把 2D 概念图、3D 渲染、剧情媒介混成风格：分别记录媒介、世界规则和状态表达。
9. 用“正常完整彩色无人”做默认规范：回到项目确认基准；结构测试方式不能决定叙事状态。

## 7. 生产交接终检闸门（局部交付仅检查适用项，讨论不以此过闸）

- [ ] 场景功能和第一/第二眼信息明确。
- [ ] 世界观线索可见且有因果，不只是装饰。
- [ ] 边界、地标、入口出口、行动路径和遮挡关系可描述。
- [ ] 存在人物时检查站立/行走/接触与尺度；无人物时检查材料/物体的运动约束与包络；纯抽象空间标物理支撑不适用并检查视觉规则。
- [ ] 相机、眼平线、透视、画幅和视觉动线服务镜头任务。
- [ ] 缩略图、静音单帧和裁切后仍能读懂主焦点与动作。
- [ ] 光源、投影、反射光、明度与天气条件一致。
- [ ] 色域与材质关系成立，风化有物理或叙事原因。
- [ ] 基准状态与剧情派生分开，版本可追溯、可回退。
- [ ] 能拆出 Hero/Supporting/Fill/Dynamic，并说明复用与动态需求。
- [ ] 已写明给分镜/3D 的不可变约束、可变字段和未决项。

单项 quality_gate.result 可使用 pass/hold/rework/pending，不替代公共生命周期、生产进度与批准层级。上游变化时直接及传递受影响产物置 needs_recheck，历史批准不作为当前有效门禁，重审后才恢复 confirmed。不要用“看起来不错”替代证据。

---

## ai-screenplay-development

---
name: ai-screenplay-development
description: "用于 AI 电影、动画及视觉作品的剧本、动作谱、场景链、对白、结构诊断和改写；新手自然地说‘帮我把点子写成故事’‘下一场怎么写’‘先试两句对白’也触发，无需完整大纲。Use for guided screenplay drafting through visible action, information and causality; world encyclopedias, character visuals and shot lists route elsewhere."
version: 0.5.0
author: Domain Knowledge Distillation
license: MIT
metadata:
  hermes:
    tags: [screenplay, narrative, character-arc, conflict, dialogue, scene-development, ai-animation]
    related_skills: [ai-visual-ideation, ai-worldbuilding, ai-character-design, ai-scene-design, ai-storyboard-design, ai-prompt-execution-contract]
---

# AI 影像剧本开发

## 新手引导与阶段成果落盘

本阶段开始时显式读取 `references/novice-guidance-protocol.md`；涉及正式状态、授权、版本、证据或交接时同时读取 `references/common-contract.md`。默认采用**新手模式**，用户可切换到协作或专业模式；模式只改变术语与讲解密度，不删除下文的剧作判断。

阶段落盘必须保存完整正文，并在写后回读通过后更新 `PROJECT_INDEX.md` 与 `SESSION_HANDOFF.md`；这三个文件及其状态必须相互一致。

- 每轮只推进一个小目标，最多问一个真正阻断当前写作的问题；其他缺口用可替换假设推进。首轮必须先给 logline 尝试、可见动作、两句对白或一小段场景等具体小产出，再问至多一个问题，不能只索要大纲。用户可答“不知道”，此时推荐一个暂定选项并继续写。
- “推荐这个”“不错”“可以”“继续”不是阶段确认，只用于更新偏好。仅当用户明确说“阶段完成”“按此定稿”或同等无歧义表述时，才记录人工确认；结构、结尾、角色选择等关键裁决不能自动代批。
- 阶段稿成熟时先交付可独立阅读的完整剧本正文。项目已有一次性“阶段确认后保存”授权时，明确确认后直接写新版本；否则把确认与保存合并为一个问题，主动提供“确认并保存为新的 Markdown 版本 / 确认但暂不保存 / 继续修改”三种自然选择。用户选择保存且目录明确后，自动创建缺失的项目基础记录文件，不要求新手手工复制或填写内部字段。阶段主文件固定为 `剧本_vNNN.md`：写前扫描同名文件，以最大三位版本号加一，首次 `v001`，不覆盖旧版；保存完整的本轮约定范围（包括必要场景正文、已定/暂定项与下游信息），不得用梗概、聊天摘要或改动列表冒充完整剧本。
- 写后回读核对，再更新 `PROJECT_INDEX.md` 的当前版本/状态/链接和 `SESSION_HANDOFF.md` 的人工决定、连续性未决项及下一步，并回读关键更新。任一步失败报告真实完成范围，不得声称保存成功；项目目录不明时只问保存位置这一项。
- 文件保存不等于阶段确认、Canon、生产批准或 research 同意，也不授权联网、上传、付费、生成或发布；需要正式交接时继续执行 `references/common-contract.md` 的状态与证据规则。

## 1. 边界与公共协议

把创意核心转成角色、世界、场景、分镜、表演、声音和制片可用的剧本；不是世界百科、小说或分镜表。使用现在时、可见可听的行动，不以抽象心理代替证据。已有确认剧本只改请求范围，不无故全稿重写。

进入阶段、交付和上游改版时加载 `references/common-contract.md`。只讨论不落盘；要求草稿可输出文本；授权落盘时确定项目目录并保留版本，不静默覆盖批准稿。缺工具交待办，不虚构生成结果。参考是条件方法，不能覆盖真实性、授权规则。

### 讨论与工作深度

按本轮意图选择讨论、探索草案、正式成稿、生产交接/验证，不新增公共 YAML 字段。讨论直接判断或局部试写，允许未知、不落盘、不要求完整公共头与生产闸门；探索可联动比较结构/人物/结尾，但标可替换假设，不冒充批准或正式 candidate。正式成稿输入足够就完整写指定范围，不以不断提问替代创作；生产交接/验证执行公共协议与证据闸门。

循环为当前意图→具体判断/试写→必要的替代与代价→吸收反馈→只改受影响项。内部记已定、暂定、否决原因、开放项，不要求每轮固定表，也允许不收敛；AI 预测不是观众实际反馈。

## 2. 模式和核心模型

正式阶段记录和下游携带 creative_mode: narrative | perceptual | conceptual 与 subject_type: character-led | environment-led | object-led | abstract；讨论中只在分支确实影响当前写作时判断，不强制先填字段。

| 模式 | 推进链 | 适用闸门 |
|---|---|---|
| narrative | 前提/主题→目标与压力→行动/反应/选择→后果→场景链 | 选择真实可用，人物知情有证据，结果回应类型与主题承诺 |
| perceptual | 感知目标→材料/空间约束→观看顺序→可观察变化→完成判据 | 注意力/光声/运动的前后差异可描述，不强要角色 |
| conceptual | 命题→对照与操作→观察差异→解释/反例→结束 | 对照条件清楚，不把隐喻演示冒充科学证据 |

无人物 `character: null` 合法，Want/Need/Lie/Ghost/对手/对白可不适用；用空间、材料、声音与动作谱写段落。基准可本来黑白受损，不擅自补救赎和彩色恢复。

通用场景模型是**功能 → 有效变化 → 后续作用**。压力型叙事可用 Goal→Conflict→Disaster 与场后 Emotion→Thought→Decision→Action，但成功、恢复、关系建立、观察和揭示场都合法；不强制每场灾难、价值反转或不可逆变化。删场后若主线、关系、认知、节奏、规则、母题和后续使用均无影响，再删或重写。

## 3. 启动与输入

判断用户要 logline/前提、人物弧线、结构、场景卡、对白、标准剧本、局部诊断还是下游交接。最小输入 payload：媒介、时长（估计/硬限制分开）、受众语气、核心/主题或感知目标、模式/主体类型、已有角色与世界规则、制作限制、请求交付、当前版本与已批准边界。

只追问会改变本轮判断或所请求正文的缺口；不影响当前局部的全片目标、规则、结尾可未知，探索可并列假设。无对白不是缺失，无人物不是缺陷。分镜与平台 Prompt 不在此阶段擅自生成或同步。

## 4. 前提与故事承诺

叙事分概念前提（谁在何种异常情境）、戏剧前提（压力下追求目标）、主题前提（选择与结果表达何种价值关系）。设计原则统一人物、世界、场景、象征和结尾。中心问题可涉及外部目标、坚持或改变、主题检验，不必每个角色纠正错误信念。

Logline 内部草拟公式：
- narrative：当催化事件发生，主角在限制下追求目标，面对阻力与赌注，以选择后果回应主题；缺陷/内在需求按弧线填写。
- perceptual：在某基准材料/空间中，光声或运动操作使注意力由初始状态移向结束状态，以可观察判据完成。
- conceptual：固定对照条件，改变一个操作，通过可见差异检验命题，注明反例/解释限制。

开头建立类型、情绪、形式、视觉或世界规则承诺，结尾回应；开放结尾可留下问题，但必须知道停止在什么可见状态。遇到‘要不要解释’先拆解释、解决状态、完成感，读 `references/structure-and-scene-chain.md` 的功能性留白：参与救场的能力边界必须成立，神秘意象不必给唯一答案。

## 5. 人物行为与弧线（仅适用时）

不写性格标签代替行为。Want 为可追踪外部目标；Need 为需要发展或坚持的价值/能力；Lie 是存在时驱动错误选择的信念；Truth 是主题提供的替代答案；Ghost/Wound 为已建立的相关过去经历；Defect/Core action 是压力下反复产生后果的策略/习惯。Lie、Ghost 允许不适用，不能为填表编创伤。

- 正弧：旧信念或策略承压→主动新选择→结果证明变化；伤口只是可选原因。
- 平弧：已有价值受到检验→坚持或更成熟地实践→影响关系/世界；不要求承认自己原先错误。
- 负弧：拒绝、腐化、失落或幻灭→行为证据与代价；不强制接受 Truth，不把悲剧自动改治愈。

对手可能是人、制度、自然或内部策略。有人格对手时写目标、合理性、优势、升级与攻击弱点；镜像是工具，不是必需身份。人物网按已有功能分配，不强加导师、盟友或反派。需要具体弧线/对手诊断时加载 `references/character-and-opposition.md`。

## 6. 冲突、选择、赌注与知情

叙事可用“追求 A→阻力→失败失去 B→绑定剂→行动后果”；赌注可以是具体的关系、任务或机会损失，不必死亡级或毁灭世界。绑定剂可以是承诺、契约、时间、封闭空间、秘密或责任，但不能代替真实选择。

Gap：预期与实际反馈不同，推动策略调整。升级可通过时间/选项减少、信息变化、盟友离开、代价提高、规则反咬完成；不要每场重复同一种失败。

涉及逃离/返回、牺牲/被迫、远景危机、门/镜面/误认时，先加载 `references/choice-and-information.md`，完成：
1. **真实选择选项表**：决策时点、选项、物理/规则可用性、角色是否知道、收益代价、最终行为、可以证明/不能证明的弧线命题。
2. **知情与感知可达表**：信息事实、观众已知、角色已知、视点/声源、距离遮挡与尺度、证据、首次发现位置。

出口已关闭不能称“放弃可用出口”；仍可写“失去出口后放弃躲藏主动救援”。观众看到远处一个人的手势，不代表角色也看到；无角色表中角色栏不适用，仍检查观众观看路径。突然赶到/预知一切时，读 `references/choice-and-information.md` 的后台行动表，先核先行动作、知情、动机、路线与主线时间，再选必要露出，不强加反派支线。同一规则引发不同动作时，读该参考的世界事实/角色解释分层；错误信念不等于规则错误。

## 7. 结构和场景链

加载 `references/structure-and-scene-chain.md` 后，按项目功能选择三幕、序列、七步、15 节拍、旅程或非叙事动作谱；写选型理由与舍弃项，不机械套页码，也不并列多套结构重复填格。

压力型参考曲线：基准→扰动→目标/承诺→旧策略尝试→信息或赌注改变→内外压力合流→决定→高潮后果→新状态。并非所有作品必须中点、低谷、Want/Need 对决。高潮可以证明坚持、拒绝或失败，不只“最大奇观”。

内部场景卡 payload：编号/地点/时间/结构功能/驱动主体与 POV；起始状态；目标或感知任务；阻力或操作约束；动作/反应/决定（适用时）；信息揭示与母题；有效变化；最后可见可听状态；后续作用。无人物省去人物弧线字段，不省去观看顺序和结束判据。

尽可能晚进入、早退出，但保留必要互动与认知过程，不用一句“他明白了”吞掉表演。邻场检查“前场输出→本场输入→本场变化→后场输入”：位置、持有物、损坏、能力和知情不能漂移，不可逆事件不重复发生。危机不能停顿时读 `references/structure-and-scene-chain.md` 的即时/压缩/延迟续场，保留最短定向与选择，将未消化反应后移并在后续行为兑现；不强制完整四步骤或每场灾难。

## 8. 对白、非语言与喜剧

对白执行攻击、防御、诱导、隐瞒、试探、命令、乞求、承认等语言行动。区分所说、知道而隐藏的未说、尚未意识到的不可说。潜台词需要真实意图及不能直说的条件，可借修理、做饭、地图等第三物争夺真实问题。

审查：想改变谁？只有此角色会这样说吗？是否向彼此解释已知事实？能否动作/沉默替代？何处有反应点？喜剧可来自认真目标、偏斜视角、重复升级与观众预期；不要求每段失败。纯形式喜剧用节奏预期和偏差，不加人物冲突。具体修改时加载 `references/dialogue-and-comedy.md`。

## 9. 诊断与旧稿恢复

故障诊断先核约束，按前提、人物/感知主体、结构、场景、对白/声音、分镜接口定位最小失效层，引用稿内证据，不只说“更电影感”；优先每轮改一主层便于归因。开放探索允许有意联动多个层并说明代价，用户直接要试写时不先强制诊断报告。反馈相互冲突或现稿偏离计划时加载 `references/diagnosis-and-rewrite.md`：分类意见并保护原始火花；先反向提纲现稿实际行动/信息/后果，再决定保留偏离还是修补，不自动拉回旧纲。

旧稿丰富度恢复必须加载 `references/diagnosis-and-rewrite.md`：先读当前确认事实与相邻场，提取旧稿互动/升级/认知/决定功能，再用当前道具、母题、能力和场界重建。旧稿不是当前 Canon，禁止顺带恢复过期名词、能力或损坏时点。先写完整表演版并诚实标估时，再考虑压缩；未经请求与批准不反向同步分镜/Clip/Prompt。

## 10. 格式与交接

需要规范正文、完整示例或制作交接时加载 `references/format-and-handoff.md`。标准格式含场景标题、现在时动作、角色对白和必要特殊格式；短句短段，可见可听，少写心理解释和镜头指令。投稿稿与制作稿的编号习惯不同，由交付要求决定，不把固定场数/时长当规范。

下游 payload：角色（适用的目标/弧线/动作习惯/关系压力）、世界/场景（规则边界/拓扑/道具/基准状态）、分镜（场功能/信息隐藏揭示/动作或观看变化/最后状态）、声音/表演（意图/沉默/声源/节奏）。无人物把角色接口标不适用，其他接口仍完整。

## 11. 公共输出模板

生产交接/验证及明确要求登记的正式记录用此头；讨论、探索、仅供阅读的 logline/诊断/局部场景稿不强制 YAML。正式成稿只交完整的指定范围，非全套 payload 配额；内部表属于 payload，不另设不兼容状态。

```yaml
artifact_id: SCRIPT-example
version: v1
artifact_type: script
creative_mode: narrative
subject_type: character-led
lifecycle_status: draft
production_status: not_applicable
approval_level: none
upstream_versions: {}
confirmed_evidence: []
open_questions: []
recheck_trigger: []
next_action: request_review
```

payload：Logline/核心；主题或感知目标/命题；类型与形式承诺；character（无人物为 null）；适用的弧线/对手/赌注；真实选项表；知情/感知可达表；结构选型；场景卡与正文；感知的材料空间约束/可观察变化/完成判据或概念的对照/指标/反例；下游 briefs；人工裁决引用与未决项。

未经人工确认、已达到公共协议候选条件的正式完成稿可为 candidate、approval_level: none；局部探索和未齐输入仍为草案，不伪造字段。确认依据必须真有引用，方向批准不等于生成/生产批准/入 Canon。上游变化令直接与传递受影响依赖 needs_recheck，保留批准历史但停止现行门禁效力；无关项经影响检查可保留并说明理由。

## 12. 最终闸门

- 模式贯通流程、模板、参考、交接；无人物未强补弧线或冲突。
- 叙事目标具体，选择当时真实可用；平弧/负弧按各自意图验收，Lie/Ghost 可不适用。
- 知情/发现符合感知距离、遮挡、声源和已给信息。
- 每场功能、有效变化、后续作用明确，而非必有 Disaster。
- 主题有行动/感知证据，结尾有可见状态；对白不成说明书。
- 当前事实和邻场连续；旧稿功能恢复未带回旧 Canon。
- 交接数据与版本真实完整；重审与批准范围明确。

核查方法迁移时加载 `references/source-map.md`；源版 skill/reference 可追溯但不作为安装外部依赖，不声称未核验原书页码。

---

## ai-storyboard-design

---
name: ai-storyboard-design
description: "Use when discussing, exploring, designing or diagnosing narrative and non-narrative AI storyboards. 支持分镜讨论与非叙事感知/概念序列，比较构图语气、视觉七要素、空间显隐和试讲反馈；按需深化 Beat、Blocking、连续性、Timing 与 Animatic 交接，不替代平台 SOP。"
version: 0.5.0
author: Domain Knowledge Distillation
license: MIT
metadata:
  hermes:
    tags: [storyboard, shot-design, visual-storytelling, blocking, continuity, timing, animatic]
    related_skills: [ai-screenplay-development, ai-character-design, ai-scene-design, ai-prompt-execution-contract, ai-video-production-classroom, ai-generation-review]
---

# AI 影像分镜设计｜完整可执行封装

## 新手逐轮引导与阶段成果落盘

任何交互先加载 `references/novice-guidance-protocol.md`；涉及状态、授权、证据、依赖或交接时同时加载 `references/common-contract.md`。真实性与用户授权优先；引导协议不削弱 Beat、Blocking、连续性、Timing、Animatic 与证据闸门。

阶段落盘必须保存完整正文，并在写后回读通过后更新 `PROJECT_INDEX.md` 与 `SESSION_HANDOFF.md`；这三个文件及其状态必须相互一致。

- 每轮只推进 **1 个小目标**，最多提出 **1 个真正阻断当前小目标的问题**。“不知道/你先建议/都可以”合法；给可撤销、标明假设的默认方案。首轮必须有小产出，例如 Scene Director Brief 的三行骨架、前三个 Beat 或两种镜头策略，不得只发输入问卷。
- 用户否决 Agent 假设时立即撤销对应项，仅改受影响范围；已有分镜/剧本默认局部修改。跨会话先读项目索引、阶段交接和最新版本恢复已定、暂定、否决及未决；不可读取时说明边界，以最小摘要继续，不假装已恢复。
- 积极反馈不等于阶段完成、保存授权或研究同意。先交完整可独立阅读的本阶段正文。已有有效持续保存授权时，只问是否确认本阶段完成，明确确认后直接保存；没有持续授权时，用一个合并问题主动提供“确认并保存为新的 Markdown 版本 / 确认但暂不保存 / 继续修改”三种自然选择，并说明预计文件名与已知目录。只有明确确认后，产物才可标 `confirmed`，否则保持 `draft/candidate`。
- 阶段确认与写文件仍分属不同授权，但不得拆成两轮重复询问。目录未知时并入同一个收尾问题；用户选择保存且目录明确后，自动创建缺失的项目基础记录文件。持续授权可撤销，不扩展到上传、付费生成、发布、研究、评测、训练或留存。
- 主文件名为 `分镜_场次_vNNN.md`。保存 Director Brief、Beat、Blocking/Floor Plan、逐镜卡、Timing/声音/接口、风险与未决项等**完整正文**，不能只存摘要或修改清单。扫描已有版本并使用下一未占用三位号，绝不覆盖旧版。
- 写后回读核对存在性、版本与正文关键段，再更新项目索引和阶段交接的路径、版本、状态、上游依赖、未决项、下一动作。任何写入、回读、索引或交接失败都报告未完成/部分完成，不报成功；Skill、文件或媒体不可用时给完整可复制 Markdown 与恢复清单，不伪造观察。
- 保存作品不等于同意研究、评测、训练、公开、上传或二次使用；这些用途须另取明确、可撤销的专项同意。

## 1. 这项工作解决什么问题

分镜不是把剧本句子逐句画出来，也不是罗列景别和运镜。分镜把戏剧行动重新组织为观众能够连续读取的视觉经验：观众先看见什么、理解什么、感受到什么，角色如何行动、反应并作出下一次选择，以及这些变化如何通过镜头、剪辑和时间被证明。

```text
剧本场景
→ Scene Director Brief
→ Story Beat
→ Blocking / Floor Plan
→ Thumbnail与视觉焦点
→ Shot Design
→ Continuity / Editing
→ Timing / Animatic
→ Layout、视频生成和动画交接
```

本技能服务叙事及非叙事 AI 短片、短动画和学生练习。它负责导演层的判断，可在探索中提出标明假设的视觉试写；不擅自改写剧本或确认场景 Canon，不替代最终平台 Prompt，也不把项目案例写成通用规则。

先选本轮交互模式：**讨论**直接回答或试写；**探索草案**允许未知、可替换假设和联动候选；**正式成稿**只完成指定范围；**生产交接/验证**或受管理产物上游改版才加载 `references/common-contract.md`，处理公共头、授权、证据和依赖复核。讨论不要求母版、动作测试、完整流程、落盘或生产包；探索候选不等于正式可交接 candidate。感知型、概念型以感知或形式变化替代人物冲突；无角色允许 `character: null`，人物字段与闸门不适用。

共同讨论循环：当前意图 → 直接判断/具体试写 → 必要的替代与代价 → 吸收反馈 → 只改受影响项。内部保留已定、暂定、否决原因和开放项，不要求固定表格或每轮收敛；输入足够就成稿，只追问影响当前判断的缺口。AI 预测不冒充观众反馈。

核心判断：七要素（空间、线、形、明度、色彩、运动、节奏）可选择主导变量跨段组织，不只换景别和时长。制作空间成立不要求观众全时清楚；平面、有限、暧昧与长镜头可主动设计。固定机位与运镜比较信息、关系和感知作用，不按主体是否位移一刀切。探索可联动，故障诊断优先单变量，均保护已批准共享约束。

## 2. 核心知识模型

### 2.1 分镜的最小单位是 Beat，不是句子

当目标、阻力、信息、情绪、关系或决定发生变化时，才形成新的 Beat。同一动作若没有新的信息或状态，不必机械切镜；但首次证据、角色 Reaction、主动 Decision 和不可逆 Result 不能为了省镜而删除。

### 2.2 每个镜头有一个 Narrative Job

一个镜头可以包含连续动作，但只保留一个主要观看任务，例如：建立空间关系、显示接触、揭示证据、承接反应、展示决定或锁定结果。景别、角度、运镜和声音只有在改变信息、关系、情绪、空间理解或节奏时才有存在理由。

### 2.3 动作是状态转换

每个动作都用以下链条检查：

```text
输入状态
→ Anticipation / 触发
→ Action Start
→ 路径与接触 / 失败
→ Result
→ Reaction
→ Decision-Action
→ 输出状态
```

“角色走过去”“他看见了”“门打开了”不是完整动作，除非明确起始位置、动力、接触或证据、结束状态和对后续的影响。

### 2.4 观众读取有顺序

每镜应说明：第一眼看什么，第二眼发现什么，何时理解变化，切点由什么触发。构图、明度、色彩、剪影、视线、运动和声音共同管理注意力，不能只靠文字解释。

### 2.5 连续性不是装饰，而是因果可读性

优先保护：

```text
情绪与故事理解 > 动作方向与眼迹 > 道具/支撑面 > 二维平面 > 三维空间细节
```

实际项目中仍需同时记录屏幕方向、轴线、人物位置、左右手、重心、道具状态和声音桥，避免用“承接上一镜”掩盖断裂。

## 3. 输入诊断与工作模式

按当前问题读取必要输入；下表用于完整设计，讨论不要求补齐全部字段：

```yaml
project:
  medium: narrative_ai_short / animation / film / previs
  audience:
  duration:
  visual_constraints:
scene:
  task:
  location:
  input_state:
  output_state:
  irreversible_change:
  audience_question:
characters:
  goals:
  obstacles:
  relations:
  core_verbs:
assets:
  confirmed_character_refs:
  confirmed_scene_refs:
  props_and_states:
open_decisions:
  -
```

根据用户意图选择一个主模式：

| 模式 | 先解决什么 | 首个输出 |
|---|---|---|
| 场级规划 | 戏剧任务、信息边界、结果 | Director Brief + Beat List |
| 逐镜设计 | 动作、焦点、镜头职责 | Shot Cards |
| 空间混乱 | 支撑面、路线、轴线、入口出口 | Floor Plan + 地标链 |
| 节奏问题 | 观看时间、Hold、动作相位 | Timing Sheet |
| 连续性问题 | 尾帧—起帧状态接力 | Transition Audit |
| 生成交接 | 资产职责和镜头执行输入 | Handoff Brief |

如果叙事因果缺口阻断当前设计，标记缺口并建议回到 `ai-screenplay-development`；讨论可先给条件方案，不因缺角色目标而阻断非叙事设计。如果确认图与旧文字冲突，以当前已确认资产和人工裁决为准，并把冲突单列为待裁决。

## 4. 按任务范围选用的方法（不是每轮强制完整流程）

### Step 1：建立 Scene Director Brief

```text
场景任务：这一场必须完成什么？
角色目标：谁主动想得到什么？
主要阻力：什么具体阻止他？
观众问题：观众在等待哪个答案？
信息分配：角色知道什么，观众知道什么？
输入状态：场景开始时人物、道具、空间是什么状态？
输出状态：场景结束时什么改变？
不可逆变化：什么不能在下一镜假装没有发生？
空间锚点：入口、出口、地标、支撑面、轴线。
信息边界：本场不得提前泄露什么？
```

质量要求：场景必须至少有一次状态或价值变化；如果只是气氛展示，要明确其视觉/感知任务，而不要强行伪造冲突。

### Step 2：拆 Story Beat

为每个 Beat 写：

```text
Beat ID：
目标 / 阻力：
可见行动：
新增信息：
情绪或关系变化：
Reaction：角色如何理解刚发生的事？
Decision-Action：角色下一步主动选择什么？
结果与下一 Beat 钩子：
```

合并重复证明，保留以下不可替代节点：首次出现、首次证据、失败、反应、决定、代价、不可逆结果。若删除一个 Beat 后后续因果完全不变，它很可能是冗余。

信息增量不只指新事件，也包括观众理解、期待、情绪、空间关系或感知节律的变化。删镜后这些作用仍完整才考虑合并；不能把有功能的静止误判为无信息。负载按注意力转移次数、陌生空间、动作相位、台词/字幕竞争和读取难度判断，不用固定秒数或镜数阈值。若负载过高，先简化竞争焦点、调整演绎时间，再决定是否切镜，并在 Animatic 中验证。

### Step 3：建立 Blocking / Floor Plan

先在平面上标记：

- 角色起点、终点和行动路线；
- 目标与阻力的方向；
- 固定道具、支撑面和可接触边界；
- 入口、出口和过渡空间；
- 摄影机可用区域；
- 屏幕左右、轴线和视线落点。

复杂空间使用地标链：

```text
上游尾帧位置
→ 第一真实落脚面
→ 可见入口/过渡边界
→ 穿越边界
→ 内部目标位置
```

“看见入口”不等于“已经进入”。需要连续展示穿越因果时，用落脚、视线或支撑面变化证明关键节点；允许有意省略过渡，但新镜须以空间锚点、声音桥或明确时间省略让观众重新定位，不把省略误写成连续运动。

### Step 4：先做 Thumbnail，再做漂亮画面

Thumbnail 的任务是测试大形和观看路径，不是完成美术。每个候选构图检查：

1. 角色、目标、阻力的关系是否一眼可读；
2. 深/平面/有限/暧昧空间线索是否符合意图，而非必须前中后景齐全；
3. 视觉焦点是否由明度、大小、轮廓、方向或对比支持；
4. 角色剪影和行动方向是否清楚；
5. 摄影机是否站在能证明动作的位置。

缩小或灰度观察仍读不出关系时，先改构图、Blocking、景别或明度，不要增加细节。

### Step 5：选择景别、角度、POV 与运镜

| 选择 | 主要功能 |
|---|---|
| 宽景 | 空间、路线、人物关系、后果 |
| 中景 | 完整动作、调度、人与物关系 |
| 近景 | 反应、表情、接触、选择 |
| 特写 | 唯一关键证据或细节 |
| POV | 某角色真实可见的信息，不等于全知镜头 |
| 高/低角度 | 改变力量、脆弱、压迫或观察关系 |
| 推拉摇移/跟拍 | 只有在信息、空间、关系、情绪或节奏发生变化时使用 |

先问“观众现在需要知道什么”，再问“用什么镜头拍”。相邻镜头若角度、焦点和画面结构完全相同，必须说明重复的功能，否则合并或制造有意义的对比。

### Step 6：写逐镜卡

```text
镜头编号 / 时长：
Narrative Job：本镜唯一观看任务。
起始状态：人物、道具、空间和重心。
构图与焦点：第一眼→第二眼，前中后景关系。
动作链：触发 → 起动 → 路径/接触/失败 → 结果。
Reaction / Decision：可见反应与下一次主动选择。
景别 / 角度 / POV / 机位：
运镜与停稳：运动由什么触发，何时停下。
声音与切点：声音来源、重音、桥接和切入切出理由。
尾帧接口：下一镜实际继承什么。
不得提前出现：
```

避免用统一套话填充“更详细”。细节只有在改变空间、动作、注意力、声音或连续性时才加入。

### Step 7：检查动作接力和镜间接口

逐对检查上一镜尾帧与下一镜起帧：

```text
位置 / 重心 / 姿态
左右手 / 道具 / 接触点
支撑面 / 屏幕方向 / 轴线
视线落点 / 动作相位 / 速度
情绪输入 / Reaction 是否已完成
声音桥 / 已完成动作是否被重启
```

前一镜—当前镜—后一镜至少一起复核。若发现断裂，先判断是 Beat 拆分问题、Blocking 问题、动作相位问题、资产状态问题还是执行描述问题，再回最小必要层。

### Step 8：制作 Timing Sheet 与 Animatic

每镜拆出：

```text
信息读取时间
Anticipation
动作启动
Contact / Failure
Result
Reaction
Hold
声音重音与切点
```

先保留完整演绎，再在 Animatic 中压缩。压缩时优先删除重复证明和无功能 Hold，不得为了适配平台时长切断失败、Reaction、Decision 或代价。镜头标题上的秒数不能替代真实动作负载检查。

计时与编号回归加载 `references/timing-verification.md`；交叉剪辑、威胁应对或表情断裂时加载 `references/threat-response-and-emotion-continuity.md`。分别记录银幕时间与故事事件时间，画外动作不能因切镜暂停；后镜继承前镜情绪，变化须有可辨识的触发或明确省略。

## 5. 诊断与回退矩阵

| 症状 | 优先排查 | 最小回退 |
|---|---|---|
| 画面漂亮但看不懂 | Scene Task、焦点、Blocking | Brief / Thumbnail |
| 人物像在等待 | 目标、阻力、主动行动、Decision | Beat |
| 动作像结果切片 | 起始状态、触发、路径、Contact | Action Chain |
| 反应消失 | 时长、镜头任务、切点 | Beat / Timing |
| 空间瞬移 | 支撑面、地标链、轴线、入口 | Floor Plan |
| 左右手/道具穿帮 | 状态、接触点、参考职责 | Continuity Ledger |
| 相邻镜头重复 | POV、焦点、角度、功能对比 | Shot Design |
| 节奏匀速 | 信息强度、静动波、Hold和切点 | Timing / Animatic |
| 镜头提前泄露结果 | 信息分配、禁止提前出现 | Beat / Scene Brief |
| 角色身份漂移 | 实际资产、参考职责、平台读取、Prompt冲突 | 先修输入，再判断是否回角色母版 |

禁止把“重写整场”作为默认修复。故障诊断优先一次只改变一个主要变量，便于归因；探索允许机位、光色、调度、顺序联动比较，保留前版和整组差量，不把方案胜出伪称单因素因果。试讲后按反馈只改受影响项，详见 `references/pitch-feedback-and-exploration.md`。

## 6. 按正式交付范围适用的质量闸门（讨论不要求生产证据）

### 场级闸门

- [ ] 场景任务、观众问题和输出状态明确；
- [ ] Beat 之间存在因果或信息推进；
- [ ] 角色有可见目标、阻力、Reaction 和 Decision；
- [ ] 不可逆变化没有被中间镜头抹平；
- [ ] 空间入口、出口、支撑面和路线成立。

### 镜头级闸门

- [ ] 每镜的主要观看职责清楚；非叙事用感知/概念任务，复杂镜内转折按读取负载验证；
- [ ] 起始状态、动作过程和结果可见；
- [ ] 第一眼焦点明确；
- [ ] 景别、角度、运镜和声音具有功能；
- [ ] Thumbnail 缩小/灰度后仍能读出关系和动作；
- [ ] 没有把未来信息提前泄露。

### 序列与 Animatic 闸门

- [ ] 尾帧到起帧状态连续；
- [ ] 角色、道具、手、支撑面和屏幕方向没有瞬移；
- [ ] 动作时长覆盖 Anticipation、Contact、Result、Reaction；
- [ ] 节奏有强弱变化而非平均切分；
- [ ] 声音、切点和视觉动作相互支持；
- [ ] 下游可以读取当前状态和真实尾帧。

证据不足时标记为“无法确认”，不得判定通过。单张截图不能证明完整视频的动作顺序、连续性或声音；必须记录已观察范围和证据时间码。

## 7. 输出与交接

根据任务只交付必要层级：

```text
场级：Director Brief + Beat List + 风险与待裁决
规划级：Floor Plan + Thumbnail逻辑 + Shot List
逐镜级：Shot Cards + 动作/反应/接口
Animatic级：Timing Sheet + Hold/切点/声音节拍
下游级：稳定镜头、资产职责、起始状态、结束状态、尾帧接口
```

生产交接及需登记状态/依赖的正式记录使用以下公共头，具体内容放 payload。讨论、探索不强制该头，正式成稿只交指定范围；不得为了填满字段编造批准或证据：

```yaml
artifact_id: SB-example
version: v1
artifact_type: storyboard
creative_mode: narrative
subject_type: character-led
lifecycle_status: draft
production_status: not_applicable
approval_level: none
upstream_versions: {}
confirmed_evidence: []
open_questions: []
recheck_trigger: []
next_action: request_review
payload: {}
```

稳定 ID 不含版本，upstream_versions 为稳定 ID 到版本的映射。确认依据只能引用真实证据或人工裁决；方向批准不代表生产批准、生成完成或进入 Canon。generated 必须有真实返回 ID 或文件。上游改版时直接及传递受影响依赖标为 needs_recheck，保留旧批准历史但不再作为当前有效门禁，重审后才恢复 confirmed；无关依赖记录影响检查理由后可保留。

## 8. 按需加载的参考资料

- `references/common-contract.md`：生产交接/验证及受管理产物上游改版时加载公共状态、证据与写入合同。
- `references/visual-structure-and-space.md`：只换景别却节奏平、构图语气比较、长镜头或主动隐藏空间时加载，选择七要素主导变化与重新定位线索。
- `references/pitch-feedback-and-exploration.md`：需要试讲、吸收实际反馈、删合调序或区分联动探索/单变量诊断时加载；缺证据时选低成本验证，不强制完整 Animatic。
- `references/timing-verification.md`：计算镜头/场景/全片时长、核编号与映射及修订回归时加载。
- `references/threat-response-and-emotion-continuity.md`：并行事件、威胁等待、情绪跳变或信息负载失衡时加载。
- `references/source-map.md`：需要定位源技能与参考资料、核来源边界时加载；来源不是执行依赖。
- `references/storyboard-methodology-map.md`：十类专业来源与方法互补关系；需要追溯知识来源时读取。
- `references/director-decision-layer.md`：观众体验、Goal/Opposition/Decision、Reaction和技法功能判断。
- `references/detailed-shot-expansion.md`：从场级结构扩写到逐镜卡时读取。
- `references/storyboard-quality-gates.md`：进行系统质量审查时读取。
- `references/shot-transition-audit.md`：逐对审查镜间尾帧—起帧时读取。
- `references/full-performance-before-timing-compression.md`：需要压缩时长或制作 Animatic 时读取。
- `references/scene-shot-size-rhythm-rebuild.md`：景别和节奏平坦时读取。
- `references/v3-action-relay-continuity.md`：动作接力或多镜动作连续性出错时读取。

这些参考文件是通用方法的深层支撑，不包含任何项目 Canon。平台 DO/Clip、素材上传、平台参数和实际视频生成交由 `ai-video-production-classroom`。

## 9. 常见误区

1. 把剧本句子逐句翻成镜头；修复：先拆 Beat 和状态变化。
2. 先选漂亮机位再寻找动作；修复：先写目标、阻力、接触和结果。
3. 一个长镜头的注意力转折读不清；修复：先调镜内调度、光色显隐和停留，仍超负载才切镜，不强制拆长镜头。
4. 删除 Reaction 和 Decision 以凑时长；修复：先删重复证明，再压缩 Hold。
5. 把确认图当作完整空间而不核对支撑面；修复：先做 Floor Plan 和地标链。
6. 用“承接上一镜”代替接口；修复：逐项写位置、姿态、道具、视线和声音。
7. 只看单张图就判定视频连续；修复：记录观察范围和时间码。
8. 生成结果改变剧情后反向修改 Canon；修复：结果先作候选，人工确认后才吸收。

## 10. 生产交接最终执行检查（讨论/局部成稿不套完整清单）

- [ ] 我当前处理的是导演/分镜问题，而非未解决的剧本或平台问题；
- [ ] 我读取了当前有效的角色、场景和道具约束；
- [ ] 我先写了场级任务或 Beat，而不是直接堆镜头术语；
- [ ] 每镜都能说明观众第一眼看什么、发生什么、结果是什么；
- [ ] Reaction、Decision、代价和不可逆变化没有被删除；
- [ ] 动作、空间、声音和时间都通过证据检查；
- [ ] 版本、状态、上游引用和待裁决项已记录；
- [ ] 输出可以直接交给下游，而不要求下游猜测缺失状态。

---

## ai-video-production-classroom

---
name: ai-video-production-classroom
description: "用于教学或执行平台中立的 AI 视频生产，把镜头变成生产组、Seedance/即梦 Clip/DO、生成请求、参考绑定、真实尾帧/文字续接、评审与剪辑交接；Use for image-to-video, text-to-video, multimodal generation, capability-based grouping, continuation, baseline logging and unavailable-generator handoffs. A single-character classroom profile is optional, never a universal prerequisite."
version: 0.5.0
author: Domain Knowledge Distillation
license: MIT
metadata:
  hermes:
    tags: [ai-video, seedance, teaching, classroom, clip, do, baseline, production]
    related_skills: [ai-storyboard-design, ai-prompt-execution-contract, ai-generation-review]
---

# AI 视频生产｜平台中立执行与课堂练习

## 新手逐轮引导与阶段成果落盘

任何交互先加载 `references/novice-guidance-protocol.md`；生产状态、授权、证据、外部执行和依赖同时加载 `references/common-contract.md`。新手节奏不能削弱平台状态机、四层生产单位、能力证据和真实提交/返回边界。

阶段落盘必须保存完整正文，并在写后回读通过后更新 `PROJECT_INDEX.md` 与 `SESSION_HANDOFF.md`；这三个文件及其状态必须相互一致。

- 每轮只推进 **1 个小目标**，最多提出 **1 个真正阻断问题**；“不知道/你先排/都可以”合法，给可撤销且标明假设的默认。首轮即交一个小产出，如首个 production_group、shot→request 映射或能力待核清单，不能只问平台参数。
- 用户否决 Agent 假设即局部撤回；已有生产表只改请求范围。跨会话先读项目索引、阶段交接、最新计划和真实证据记录；不可读就说明缺口，以最小摘要继续，绝不补造授权、平台状态或句柄。
- 积极反馈不等于阶段完成、保存、上传、付费提交、人工通过或研究同意。先交完整可执行生产计划。已有有效持续保存授权时，只问是否确认本阶段完成，明确确认后直接保存；没有持续授权时，用一个合并问题主动提供“确认并保存为新的 Markdown 版本 / 确认但暂不保存 / 继续修改”三种自然选择，并说明预计文件名与已知目录。只有明确确认才将**计划产物**标 `confirmed`，计划 confirmed 不会把请求状态提升为 submitted/generated/reviewed。
- 阶段确认与保存授权不得互相推导，但不得拆成两轮重复询问。目录未知时并入同一个收尾问题；用户选择保存且目录明确后，自动创建缺失的项目基础记录文件。上传、付费、提交、重试、发布和研究必须按动作另行授权，持续保存授权不能代替。
- 主文件名为 `视频生产计划_vNNN.md`，保存输入冻结、四层映射、能力依据、完整请求/回传需求、状态、风险和下一动作的**完整正文**，不只存摘要。真实提交快照、上传回执、任务 ID、返回 ID/文件、错误和观察证据必须另建证据记录并引用；只有实际取得才写，绝不可把计划字段复制成证据或伪造生成结果。
- 写前扫描版本并使用下一未占用三位号，旧计划/批准稿不覆盖；写后回读核对，再更新项目索引与阶段交接。写入、回读、索引、交接任一步失败只报未完成/部分完成。生成工具、Skill、文件或媒体不可用时，仍交完整计划和生成请求/外部回传清单，但不声称上传、提交、生成、观看或导出。
- 保存作品不等于同意研究、评测、训练、公开、上传、留存或二次使用；专项用途另取明确可撤销同意。

## 1. 通用目标与可选课堂 profile

本技能把已明确的镜头/视觉变化转为可追踪生成请求，完成输入冻结、能力选择、提交、回传、评审及剪辑交接。不包含平台私有 SOP，不定义项目故事或美术设定，也不强行将所有任务缩成单角色短片。

`profile: general` 是默认。用户选择 `single-character-classroom` 时才采用“一个已确认角色、一个主要场景、一个主要动作、一个可见结果”的小练习约束。这是控制教学难度，不是通用生产闸门。也可选择 environment-study、object-study、abstract-study，或保持 general。

携带 creative_mode 与 subject_type；environment-led/object-led/abstract 的 `character: null` 完全合法。叙事作品可记录 Narrative Job；感知/概念任务记录感知目标、材料/空间约束、可观察变化和完成判据。静态资产不必通过视频练习，交提示词静态分支。

先判断工作深度：讨论时可直接解释能力条件或局部试排，不提交、不要求完整生产记录；探索草案可比较长短请求、分组或桥接方案，明确哪些只是待验证假设；正式成稿只交付用户指定的课堂练习、请求表或交接范围。仅在生产交接/验证、明确要求登记受管理产物，或受管理上游换版时加载 `references/common-contract.md`，状态、证据与写入使用公共协议。落盘先确定授权工作区、保留历史版本。项目明确规则可覆盖时长和格式偏好，不能覆盖授权及证据真实。

## 2. 条件加载路由

| 条件 | 加载文件 | 预期产出 |
|---|---|---|
| 进入生产交接、验证、无工具受阻或基线比较 | `references/production-modes-and-baseline.md` | 能力证据、执行/等待/失败状态、实验账本 |
| 导演镜头转 Clip/DO、任务合同、重试/桥接/合并映射 | `references/production-grouping-and-task-contract.md` | shot→production_group→request→edit_unit 四层映射 |
| 目标为 Seedance/即梦或需核验平台能力 | `references/seedance-capability-and-evidence.md` | 生成模式、动态能力档案、官方/经验/实测证据分层 |
| 镜头分组、长短请求、续接与剪辑 | `references/clip-continuity.md` | 四层映射、能力矩阵、接口和回退策略 |
| 真实尾帧、文字重启、长窗桥接或剪辑交付 | `references/continuation-and-edit-delivery.md` | 接口资格、EDL、来源追溯和输出验证 |
| 课堂演示、无角色或受阻交接 | `references/worked-examples.md` | 可交付实例与上游改版影响链 |
| 溯源或核对是否夹带私有规则 | `references/source-map.md` | 源版位置、通用化范围 |

提交文本由 `ai-prompt-execution-contract` 组装；真实结果交 `ai-generation-review`。这些是包内技能，不依赖源版安装路径。

## 3. 进入条件与输入冻结

1. 记录本轮授权：分组、写稿、上传、付费生成、审查、剪辑分别确认。已有镜头可局部进入，不必重走全流程。
2. 引用必要上游稳定 ID→版本及当前批准证据；候选输入可用于授权探索，不能标生产批准。无角色不要求角色资产；纯文字生成可无参考图，但空间/材料/变化仍应明确。
3. 冻结任务、主体白名单/范围、已确认基准状态、目标画幅、导演时长意图、可读结果及不可修改范围。废墟可就是基准，不要求先生成“完整正常态”。
4. 核对文件可访问、内容已视觉观察与实际上传状态，不能凭文件名填画面。素材不在本环境就请求回传或交外部人工核验。

## 4. 四层生产单位与能力条件矩阵

- **shot / 镜头**：导演观看、构图、动作和切点单位。
- **production_group / 生产组**：共享一个主要任务、连续空间/状态和参考集合的生产规划单位；Clip/DO 可作显示别名。
- **edit_unit / 剪辑单元**：后期采用的一段，记录来源请求和 in/out；可能横跨镜头，也可能只是镜头一部分。
- **generation_request / 生成请求**：实际一次平台调用，具有输入快照、参数、任务句柄、输出和失败状态。

分开 ID，显式映射。不要把一个 Clip/DO 同时当成镜头、生产组、剪辑段和请求。一个生产组可有多个重试/补段请求，一个请求也可覆盖多个相邻生产组。导演目标时长、平台生成窗口、实际输出时长和成片采用时长分别记录；无实际结果时裁切是 planned，不写成已经执行。

每个生产组建立一个任务合同：叙事任务使用 Narrative Job，感知任务使用 perceptual goal，概念任务使用 conceptual test；都要有可观察完成判据、可见终点和不得提前出现的信息。若两个不可替代结果争夺同组，拆组或请人工明确主从。

| 已核对的条件 | 优先候选 | 风险与退出条件 |
|---|---|---|
| 平台支持目标长窗口，空间/材料基线连续，跨段运动相位重要，额度允许比较 | 一次较长请求，内部保留镜头/变化节点 | 长段漏动作或漂移→定位失败区间，再短段/桥接；不自动替换已批准版本 |
| 已观察到长段高失败率，复杂接触/状态多，平台窗口不足或局部重试成本低 | 按可观察状态边界拆短请求 | 接口速度/身份/光色不连续→桥接或受控合并；不压缩必要结果证明 |
| 多镜头能力未知或只有单镜能力证据 | 按单镜/单变化试验，或等待核验 | 不声称短段必然稳定；仍保存结果验证 |
| 前后主体已通过，仅边界有问题 | 短桥接、局部补段或后期切点 | 不无故整段重生成；不能用剪辑掩盖关键因果错误 |
| 参数/窗口/槽位信息缺失 | 平台中立方案与待查项 | 不固定秒数，不先宣称可生成 |

平台能力必须记录来源、核对时间、模型/版本及适用模式；文档支持不等于项目已实测稳定。窗口余量用于可裁 handles 或既有动作闭合，不添无功能行走或重复反应。改变导演内容须退回分镜确认，而非生产层擅改。

## 5. 执行、等待与基线

1. 从能力矩阵选请求边界，列覆盖镜头及计划剪辑单元。
2. 按参考图事实→本镜读取范围→禁止继承指定职责；整图拓扑/权威全貌构图可用，避免无关图或冲突状态。抽象效果必须具备范围、材质、运动路径和终点。
3. 组装完整提交层；不假定平台读得到本地路径、项目常量或前次对话。平台有真实上下文继承时保存来源句柄。
4. 提交前确认素材资源可用、参数受支持、额度/授权足够；独立请求可在工具和授权允许时并行，依赖真实尾帧的请求等待上游真实且合格的接口。
5. 保存实际提交快照、上传回执、模型/参数、调用时间、任务 ID、返回资源/文件和错误。不将 submitted 当 generated；仅任务句柄而无输出时仍在 submitted。
6. 第一轮真实可访问输出是生成基线，不是预期描述；它即使失败也保留。已通过基线和新候选分开，不把“用户喜欢旧版”外推为普遍模型规律。
7. 转评审，记录观察范围和人工待裁决；没有视频只能做合同审计，不能判断动作/声音。

| 实际情况 | 生产状态/下一步 |
|---|---|
| 无工具，外部待生成 | awaiting_external_generation；交完整输入、计划素材和回传清单 |
| 必需素材缺失 | planned；blocked_reason 写具体资产及受阻职责，请回传；纯文字替代须授权另版 |
| 用户拒绝提交/预算 | planned 或 not_started；保留拒绝证据，await_user_authorization，不代操作 |
| 返回受理 ID 无视频 | submitted；查询任务，不伪造生成文件 |
| 明确错误/拒绝/生成失败 | failed；记录错误，先检查已受理情况再决定授权重试 |
| 超时且不知是否受理 | submitted（已知任务 ID）或 planned（无受理证据）；标 submission_uncertain，先查状态避免重复扣费 |
| 真实结果可访问 | generated→under_review；以实际观察推进 |
| 等待人工 | 保留真实生产状态；human_decision: pending，await_human_review |

## 6. Clip / 请求生产记录模板

本记录跟踪一个请求及它的计划/实际剪辑映射，标题 Clip 不再替代字段语义。公共头所有字段保留。

```yaml
artifact_id: CLIP-example
version: v1
artifact_type: clip
creative_mode: perceptual
subject_type: environment-led
lifecycle_status: draft
production_status: planned
approval_level: none
upstream_versions: {}
confirmed_evidence: []
open_questions: []
recheck_trigger: []
next_action: verify_capabilities
payload:
  profile: general
  character: null
  target_goal: null
  completion_criteria: []
  authorized_actions: []
  authorization_evidence: null
  protected_scope: []
  shot_ids: []
  edit_units: []  # edit_unit_id、source_request_id、planned/actual in-out
  production_group_id: PG-example
  display_alias: DO-example
  task_contract: {type: perceptual_goal, primary_job: null, observable_end_state: null, forbidden_early_reveal: []}
  generation_requests: []  # request_id、role(primary/retry/bridge/merged_test/pickup)、status
  director_duration_target: null
  requested_window: null
  actual_output_duration: null
  capability_checks: []
  reference_duties: []
  actual_uploaded_assets: []
  planned_but_not_uploaded: []
  complete_submission_text: null
  actual_submission_snapshot: null
  platform_model_parameters: {}
  returned_task_id: null
  returned_output_id: null
  result_file: null
  error_or_block: null
  baseline: null
  allowed_changes: []
  coupled_repair_rationale: null
  continuation_interface: null
  review_record: null
  human_decision: pending
  human_decision_evidence: null
```

## 7. 回归、交付和版本失效

默认只改一个主变量便于归因，但并非永远只能一个：相冲突的素材和文字状态、相互依赖的起点与下游接口可联合修复。写清联动依据、允许变化、受保护版本和回归项；单次随机生成不能证明根因或普遍优越性。先控制平台/模型/参数与素材，再重复可负担的比较；种子不可控则明确混杂。

上游改版追踪直接及传递受影响依赖→`needs_recheck`，历史批准保留但不再作为有效门禁；记录无关项保留理由。重审后恢复 confirmed，不因旧片已生成就认定它符合新稿。

交付真实成片时记录来源资源、采用版本、裁切范围、替换段与声音决定，验证播放、画幅、时长和音轨。未做的检查标未验证；没有剪辑工具就交剪辑决策表，不宣称成片已导出。

- [ ] profile 已声明，非叙事/无角色不被课堂角色约束拦截。
- [ ] shot、production_group、generation_request、edit_unit 四层分开，长短选择有能力条件而非固定秒数。
- [ ] 每个生产组有一个主要任务合同和可观察终点；DO 只是可选别名。
- [ ] Seedance 能力带来源、日期、模型、产品入口和模式；官方事实、案例归纳、第三方经验、项目观察和人工接受未混写。
- [ ] 输入版本、基准状态、可见终点和参考职责明确。
- [ ] 计划/上传/受理/生成/评审/人工接受分开，有真实证据才晋级。
- [ ] 缺工具、缺素材、提交失败、拒绝和人工等待各有出口。
- [ ] 修复范围受控，耦合修复已说明，失败与通过版本均保留。
- [ ] 尾帧/文字重建接口诚实标来源，坏接口不继续传播。

---

## ai-visual-ideation

---
name: ai-visual-ideation
description: "用于 AI 电影、动画及视觉作品的创意起步、氛围、核心意象与方向比较；新手自然地说‘我只有一个模糊画面’‘不知道拍什么’‘帮我把这个点子展开’也触发。Use for guided ideation and concrete micro-drafts without requiring a full brief; screenplay rewrites and world mechanics route to their specialist skills."
version: 0.5.0
author: Domain Knowledge Distillation
license: MIT
metadata:
  hermes:
    tags: [ideation, creative-core, concept-development, ai-animation, brainstorming]
    related_skills: [ai-creative-workflow-router, ai-screenplay-development, ai-worldbuilding]
---

# AI 影像头脑风暴

## 新手引导与阶段成果落盘

本阶段开始时显式读取 `references/novice-guidance-protocol.md`；涉及正式状态、授权、版本、证据或交接时同时读取 `references/common-contract.md`。默认采用**新手模式**，用户可切换到协作或专业模式；模式只改变讲解密度，不削弱下文的创意判断与专业闸门。

阶段落盘必须保存完整正文，并在写后回读通过后更新 `PROJECT_INDEX.md` 与 `SESSION_HANDOFF.md`；这三个文件及其状态必须相互一致。

- 每轮只推进一个小目标，最多问一个真正阻断该目标的问题；其余缺口标为可替换假设。首轮先根据现有描述给一句创意核心、一个微型画面发展或两个有实质差异的短方向，再问至多一个问题，不能只索要资料。用户可回答“不知道”，此时给推荐假设并继续。
- “推荐这个”“不错”“可以”“继续”只表示偏好或继续探索，不是阶段确认。仅当用户明确说“阶段完成”“按此定稿”或同等无歧义表述时，才记录人工确认；不得自动代替关键创意裁决。
- 阶段稿成熟时先交付可独立阅读的完整创意正文。若项目已有一次性“阶段确认后保存”授权，明确确认后直接写新版本；否则把确认与保存合并为一个问题，主动提供“确认并保存为新的 Markdown 版本 / 确认但暂不保存 / 继续修改”三种自然选择。用户选择保存且目录明确后，自动创建缺失的项目基础记录文件，不要求新手手工复制或填写内部字段。阶段主文件固定为 `创意核心_vNNN.md`：写前扫描同名文件，以最大三位版本号加一，首次为 `v001`，不覆盖旧版；正文必须完整包含本阶段适用的核心、模式、主意象、限制、选择/未决项和下游接口，不能只存摘要或修改说明。
- 写入后必须回读核对，再更新项目目录中的 `PROJECT_INDEX.md`（当前版本、状态、链接）与 `SESSION_HANDOFF.md`（本轮人工决定、暂定/否决项、下一步），并回读关键更新；任一步失败如实报告，不得报保存成功。目录不明且确需保存时，只问这一个阻断问题。
- 保存、阶段确认、Canon/生产批准及 research 同意彼此独立。落盘不自动入 Canon、不自动提升 lifecycle/approval、不授权研究、联网、上传、付费、生成或发布；受管理交接继续依 `references/common-contract.md`。

## 1. 边界与公共协议

把空白、混乱或过载材料转成一个可比较、可选择、可继续制作的创意核心。生成很多点子不等于完成；不替用户未经确认地决定主题、故事或 Canon。已有剧本和镜头任务可跳过本阶段。

进入阶段、交付和上游改版时加载 `references/common-contract.md`。用户只讨论则只答复；要求草稿可输出文本；授权落盘后确认当前项目目录，保留版本，不静默覆盖批准稿。缺工具时交付待办，不编造生成结果。正文是通用默认，参考是条件方法；项目授权可改风格/时长/格式，不能覆盖真实性和授权要求。

### 讨论与工作深度

按本轮意图选择讨论、探索草案、正式成稿或生产交接/验证；这是对话概念，不新增公共 YAML 字段。讨论直接判断或试写，只问影响当前问题的缺口，可保留未知、不落盘、不套完整公共头与生产闸门。探索允许可替换假设及联动候选，不等于正式 candidate；正式成稿只完整交付指定范围，输入足够就直接写；生产交接/验证才完整执行公共协议与证据闸门。

共同循环：当前意图→直接判断/具体试写→必要时少量替代与代价→吸收反馈→只改受影响项。内部保留已定、暂定、否决原因、开放项，不要求每轮输出表或强行收敛。AI 预期不冒充真实观众反馈。若意见冲突，读 `references/selection-and-counterproof.md` 的愿景过滤；日常切片卡住读 `references/raw-material-and-observation.md` 的双处境原型；转正式交接读 `references/project-box-and-handoff.md`。

需要组织候选或交接时确定 creative_mode 与 subject_type；讨论不为填字段打断创作：

| 模式 | 必需结构 | 不强制 |
|---|---|---|
| narrative | 主体、欲望、阻力、可选行动与后果 | 创伤、反派人数、正向成长 |
| perceptual | 感知目标、材料/空间约束、观看路径、可观察变化、完成判据 | 人物、欲望、冲突、道德选择 |
| conceptual | 命题、对象、对照/操作、可观察指标、反例与最小测试 | 人物弧线、戏剧危机 |

subject_type 为 character-led / environment-led / object-led / abstract；无人物 `character: null` 合法，不能拟人化窗帘/空间来补欲望。黑白、受损、无对白都是合法基准，不擅自彩色化或修复。

## 2. 核心模型与输入

真实观察/记忆/限制/奇观 → 材料卡 → 核心问题 → 受控候选 → 反证比较 → 人工选择 → 最小测试 → 下游接口。

AI 是变体机器、追问者和反证者，不是创意裁决者。先保存人的材料再扩展；生成与评审分开。只问会改变方向的缺口，其余作为可替换假设，不逼用户解释私密经历。

输入 payload：原始冲动/画面、在乎原因（可不确定）、已有观察/记忆/参考/AI 输出、媒介与受众、硬限制（时长/资产/地点/色彩/预算）、明确不想要、请求范围。内部材料表附属于阶段记录，不另发明状态字段。

## 3. 执行流程

### Step 1：原始材料与启动阻力

材料不足可做一次短自由写或眼前观察：不删改、不解释，记录具体物、动作、声音、气味、光线。记忆抓快照，不要求完整自传；AI 示例标成虚构，不冒充用户经历。

每卡记录：类型（观察/记忆/物件/动作/关系/问题/失败版本）、内容、吸引注意的具体细节、可能用途、相关限制、来源。身体反应或欲望只是可选线索。

启动卡住先识别可观察问题：不知道产物→限定一张卡；材料缺失→观察或获取获授权材料；任务过大→缩为一物一变化；边写边否定→先分离生成与评审。可提议短时间盒，但用户可拒绝/暂停，不作心理诊断。加载 `references/raw-material-and-observation.md` 获取练习和失败分支。

### Step 2：显影核心问题

- narrative：谁想得到/避免什么？为什么现在行动？哪些行动可选？失去什么？选择如何回应主题？
- perceptual：注意力先落哪里？什么材料/光声条件在变化？观众何时重新看懂空间？何时结束观察？
- conceptual：命题是什么？固定什么、改变什么？什么差异支持或反驳命题？视觉演示不能证明什么？

输出主题问题、感知目标或待检验命题，不停在“爱、孤独、科技”等词。加载 `references/theme-and-core-image.md` 解决只有氛围或主题过大的问题。

### Step 3：形式与限制

判断需要一句话、一张图、一场戏、短片、系列、拼贴或交互。让限制成为发生器：限制阻止什么、逼迫强调什么，能否变成材料规则、动作或视觉语言？不默认线性故事或人物弧线。保留形式与时长为假设，未实测不当成平台能力。

### Step 4：受控变体

通常给出少量方向供比较；复杂项目可先短变体再选少数深化。每方向填写：核心一句话、不可替代的图像/体验、模式对应结构、限制、实际可观察变化、形式/时长假设、制作风险、结束/完成状态。只有“赛博朋克、电影感、神秘”等形容词时退回材料。

### Step 5：反证与筛选阻力

分别用怀疑者、结构编辑、目标观众、制片视角检查：删最酷设定还剩什么？行动/观看变化是否不可替代？是否只拼热门作品？限制是否改变结果？资源能否取得？概念路径有何反例？

保留具体、可继续、受限且可测试的方向。堆肥不是删除：保存不采用原因与来源。无人物不因“角色可替换/无欲望”淘汰。方向过载时冻结新变体，用同一组指标比较；同分则测试差异最大的一个条件或交人工选，不假装评分等于批准。加载 `references/selection-and-counterproof.md`。

### Step 6：人工裁决与完成阻力

记录人工选择、否定理由、未锁定项和待测假设。未选择则只交候选。选定后压缩成一段核心、一个问题/目标、一个主视觉母题和最小可测试版本。

收尾反复增加设定时锁定本次完成定义：只修阻碍交接的问题，其余进后续清单；无授权不发布。若实际资源不够，交低成本测试或等待，不把等待诊断为懒惰/完美主义。加载 `references/project-box-and-handoff.md` 得到项目盒、明日桥和交接步骤。

## 4. 输出合同

生产交接/验证及明确要求登记的正式阶段记录先附同一公共头，内部卡片是 payload；讨论、探索草案和仅供阅读的核心卡/比较答复不强制 YAML。正式成稿按指定范围完整给正文，不自动扩成整阶段交接。artifact version 与技能版本分离。示例为未确认感知项目：

```yaml
artifact_id: IDEA-window
version: v1
artifact_type: idea
creative_mode: perceptual
subject_type: environment-led
lifecycle_status: draft
production_status: not_applicable
approval_level: none
upstream_versions: {}
confirmed_evidence: []
open_questions: []
recheck_trigger: []
next_action: request_review
```

payload：
- 原始材料与来源、核心问题/主题或感知目标。
- character: null（无人物）；叙事才填主体/欲望/阻力/真实选项。
- perceptual：材料空间约束、观看路径、可观察变化、完成判据。
- conceptual：命题、对照、观察指标、反例、解释边界。
- 核心图像、形式/时长与硬限制、候选比较。
- 人工选择及实际证据引用；否定方向和原因；可替换假设。
- 最小测试、下游接口、下一步动作。

只有达到公共协议候选条件、可供正式审阅的记录才用 lifecycle_status: candidate、approval_level: none；探索中的几个方向不自动升 candidate，未齐输入保持草案。方向批准不代表生产批准、已生成或入 Canon。上游换版对直接/传递受影响依赖标 needs_recheck，旧批准保留历史但不作现行门禁，无关项经影响检查保留并写理由。

## 5. 质量闸门、故障与交接

- 具体材料和来源保留，AI 虚构与用户观察分开。
- 模式从输入贯通步骤、输出、交接；不适用字段明确而非硬填。
- 叙事选择/后果，感知观看变化，概念操作/证据分别可说清。
- 限制进入形式或结果；需要选方向时给有实质区别的少量候选，用户只问一个点或已指定方向时直接深化，不为配额造变体。
- 人工选择或暂不选择如实记录；最小测试和完成判据明确。
- 无材料→观察；无差异→增加限制/两物碰撞；只有氛围→具体材料与观看路径；事件过多→压缩一个变化；无限生成→比较；收尾扩张→锁范围。

交剧本：核心、模式/主体类型、主题/感知目标/命题、适用的主体欲望、形式限制和未决项。交世界：必须解释的异常、材料空间规则和基准状态。交角色/场景/分镜：只把有依据的功能作为约束，候选只能作探索输入，不能冒充 Canon。非叙事可直接去场景/分镜，世界观可按需缩为一张约束卡。

## 6. 参考入口

上述专项加载入口均按问题调用，不必每次全读。不知从何开始时加载 `references/methodology-map.md` 选失效环节；核查迁移来源时加载 `references/source-map.md`，来源是追溯记录而非运行依赖。

---

## ai-worldbuilding

---
name: ai-worldbuilding
description: "用于 AI 电影、动画及视觉作品的世界规则、资源制度、历史文化、时空与设定一致性；新手自然地说‘帮我搭一个世界’‘这里的人怎么生活’‘这个机制合理吗’也触发，无需先写世界圣经。Use for guided worldbuilding and small rule-testing drafts; do not silently canonize proposals or take over screenplay, character visuals or composition."
version: 0.5.0
author: Domain Knowledge Distillation
license: MIT
metadata:
  hermes:
    tags: [worldbuilding, visual-development, rules, spatial-logic, canon, ai-animation]
    related_skills: [ai-screenplay-development, ai-character-design, ai-scene-design, ai-storyboard-design, ai-prompt-execution-contract]
---

# AI 影像世界观

## 新手引导与阶段成果落盘

本阶段开始时显式读取 `references/novice-guidance-protocol.md`；涉及正式状态、授权、版本、证据或交接时同时读取 `references/common-contract.md`。默认采用**新手模式**，用户可切换协作或专业模式；切换只改变讲解密度，不删减下文的规则、空间与一致性判断。

阶段落盘必须保存完整正文，并在写后回读通过后更新 `PROJECT_INDEX.md` 与 `SESSION_HANDOFF.md`；这三个文件及其状态必须相互一致。

- 每轮只推进一个小目标，最多提出一个阻断问题；其他未知写成可替换假设。首轮先给一条可运行规则、一段由规则产生的日常或一个具体一致性判断，再问至多一个问题，不能只发设定问卷。用户可回答“不知道”，随后采用清楚标注的推荐假设继续。
- “推荐这个”“不错”“可以”“继续”是偏好或探索指令，不是阶段确认。仅有“阶段完成”“按此定稿”或同等明确表述才构成人工确认；规则、历史、Canon 与例外等关键裁决不得自动代批。
- 阶段稿成熟时先给可独立阅读的完整设定正文。项目已有一次性“阶段确认后保存”授权时，明确确认后直接写新版本；否则将确认与保存合并成一个问题，主动提供“确认并保存为新的 Markdown 版本 / 确认但暂不保存 / 继续修改”三种自然选择。用户选择保存且目录明确后，自动创建缺失的项目基础记录文件，不要求新手手工复制或填写内部字段。阶段主文件固定为 `世界观设定_vNNN.md`：写前扫描同名文件，以最大三位版本号加一，首次 `v001`，不覆盖；正文完整保存约定范围内的基准、规则机制/边界/证据、已定/暂定/禁用项和下游接口，不用百科摘要或修改列表代替。
- 写后回读，再更新 `PROJECT_INDEX.md` 的当前版本/状态/链接和 `SESSION_HANDOFF.md` 的人工裁决、冲突/未决项、下一步，并回读关键更新。任何一步失败均报告实际状态，不得报成功；保存目录未知时只补问该阻断项。
- 保存不等于阶段确认、Canon/生产批准或 research 同意，不授权联网、采集、上传、付费、生成或发布；正式交接与依赖变更继续依 `references/common-contract.md`。

## 1. 边界与公共协议

把故事、视觉概念或现实题材的必要设定转成影响行动、空间、资产、声音和生成结果的规则系统。不是百科；风格不自动等于规则。现实项目可只建当前视觉、空间、时间约束表，不为短片强写长篇世界史。

进入阶段、交付和上游改版时加载 `references/common-contract.md`。用户只讨论则只答复；授权落盘时确认项目目录、保留版本，不静默覆盖批准稿。缺工具交待办，不虚构生成结果。正文为通用默认，reference 为条件方法；项目可授权改风格/格式/时长，不可覆盖真实性和授权。

### 讨论与工作深度

按意图选择讨论、探索草案、正式成稿、生产交接/验证，不新增公共 YAML 字段。讨论直接判断或局部试写，未知可保留，不落盘、不强求公共头与生产闸门。探索可比较联动规则与生活候选，假设可替换，不等于正式 candidate；正式成稿只完整写用户指定范围，输入足够即写；交接/验证才执行完整公共协议与证据闸门。

循环为当前意图→具体判断/试写→必要的替代与代价→吸收反馈→只改受影响项。内部记已定、暂定、否决原因、开放项，不每轮输出固定表，不强迫收敛；AI 推测不能称受众反馈。转交接时读 `references/visual-state-and-handoff.md` 的分层方法。

正式记录与下游传递 creative_mode: narrative | perceptual | conceptual 与 subject_type: character-led | environment-led | object-led | abstract；自然讨论不为填字段中断回答。无人物 `character: null` 合法：
- narrative：规则→使用/受压→选择→后果→证据。
- perceptual：感知目标→材料/空间约束→光声运动/观看路径→可观察变化→完成判据。
- conceptual：命题→规则/对照操作→可观察差异→支持或反例及解释边界。

世界基准可以本来受损、黑白、断电、抽象；不自动改成“正常、完整、彩色”。没有人物不必造欲望、对手或社会冲突。

## 2. 输入与核心模型

判断任务：现实约束、局部地点、幻想机制、文化社会、历史、地图、视觉语言或一致性诊断。输入 payload 包含媒介/受众/形式/制作限制、核心或剧本动作/感知要求、角色（可 null）、已有事实、提案、禁用假设、当前版本和请求范围。

核心链：差异/基准→运行机制→边界与适用后果→空间/资产/声音证据→一致性→下游交接。社会关系仅适用时加入受益/受害分析。

已确认事实、待裁决候选、灵感材料分开；灵感是材料分类而非公共生命周期枚举。模型输出和生成图片不能自动进入 Canon。世界类型可为现实叠加、隐藏/平行或独立世界；入口/回归机制只在存在跨界时建立，不强加传送门。

## 3. 最小规则集

先回答与现实或项目基准相比最关键的差异，再追踪它对自然、资源、技术、日常、文化和空间的实际影响，不全量填百科。

内部规则卡 payload：规则ID、机制/来源、使用/触发条件、谁可操作（适用时）、边界/失败方式、代价或物理后果、受益/受害/排除者（社会适用时）、人物选择或感知/概念操作、可见可听证据、禁止推断、未决项。

“神秘、古老、压抑”只在风格栏；要成为规则须说明改变什么可观察条件。能力应有限制、代价或后果；感知规则不必有道德代价。装饰合法，但不得暗中取得机制能力。

- 技术/魔法：能力→条件→限制→代价/后果，检查资源、维护、误用和失效。
- 社会：规范→执行者→获益/排除→违规后果，不把制度写成全体一致信仰。
- 自然：条件→材料/资源→物理/生态限制→适应行为或可观察效果。

规则至少服务选择/阻力、空间路径、资产、光声材料或概念对照之一；都没有则降为背景灵感/装饰，不为补作用滥加惩罚。

## 4. 母题权限与能力边界

花、水、线、风、晶体等重复元素先明确是装饰、指示器、载体还是源头；允许组合但逐项授权：
- 装饰只负责视觉重复，不能因画了花纹就解锁能力。
- 指示器显示其他系统状态，不改变系统。
- 载体传递/储存既有量，写容量、损耗、路径，不凭空产生。
- 源头确实产生量或状态，写来源条件、产出上限、代价与停止机制。

写“根本原因→指示/承载→日常用途”，危机和恢复尽量沿同链展示；日常用途不越权取代根因。特殊生物先选唯一个体/临时显形/生态现象/物种/文明，按项目需要，不默认扩成王国。需要具体权限表与反例测试时加载 `references/rules-costs-and-ethics.md`。

## 5. 基础设施与条件历史深度

按需求建立空间（边界、入口/出口、可达性、移动/观察规则）、时间（当前时点、周期、前史、变化来源）、社会（劳动、交换、照护、禁忌）、资源（来源、稀缺、维护、分配）、文化（语言、仪式、服饰、日常）、生态/技术（生物链、普及、故障）。无居民/抽象项目可标社会文化不适用。

历史深度不是由“梦境/模拟”标签决定。先选：独立演化历史；生成且无独立前史；生成但继承/模拟历史；未知。第二分支才约束不能从生活设施自动推导王朝/宗教史；第三分支可有遗迹，但区分实际过去与内置记忆。未知保留假设，不一刀切禁用古迹。加载 `references/culture-timeline-and-canon.md` 执行历史分支、文化推导和时间线案例。

地图记录入口出口、路径方向、门槛、遮挡与时间条件。社会叙事再标谁使用/被排除及关键选择；无人物标风、光、声音和观众视线传播。地点作为“第三角色”是比喻，不要求空间有欲望。加载 `references/spatial-and-environmental-evidence.md` 做拓扑、旅行时间和环境证据检查。

## 6. 环境叙事与视觉语言

从磨损、维修、物品分配、废弃设施、标识、障碍、声源、光周期与移动方式传达世界，不让说明对白承担全部。道具可为欲望对象、秘密载体、关系信物、阶层/前史证据、铺垫兑现、冲突工具、视觉母题或纯装饰；分别标职责。

形状/构图说明秩序、隔离或流动；材质/磨损说明使用、时间或既定基准；光色/能见度说明观察条件、危险或信息控制；声音说明空间、生活或异常。母题写权限、第一次建立、后续观看/行动功能与结尾状态，不强要参与冲突。

只用**项目确认的基准状态**。母版可本来废墟黑白，派生仅指相对基准的变化（例如原有裂缝上新增积水，而不是“废墟”本身）。记录触发、前后状态、持续性与回退条件；不可逆或无需恢复的项目回退可不适用。加载 `references/visual-state-and-handoff.md` 执行状态账本和交接。

## 7. 世界与剧本/动作谱闭环

叙事检查规则如何改变选择，违反既定规则如何反馈，对手/主角如何使用不同策略；不要用规则替代动机。perceptual 检查材料/空间约束、观看路径与可观察完成状态，允许世界不被改变而观众认识改变。conceptual 检查对照操作是否受同一规则约束，不擅自赋予比喻真实因果。

当前故事实际使用的关键规则绑定场景/段落及必要可见可听证据；不要求所有后台事实出镜。不要靠加入新规则解释旧漏洞；要么澄清已有边界，要么提案改版并等待裁决。同一规则的不同解释或日常缺乏质感时，读 `references/culture-timeline-and-canon.md`：固定世界事实，改变知情与利益位置，试写不同动作；用同一早餐/维修的两种处境比较，不再扩写世界总史。谜团应保留到哪里也读该参考：机制因果要成立，不必解释全部来源。突然到场或幕后失效则读 `references/spatial-and-environmental-evidence.md` 的后台因果走查。

## 8. 一致性与异常处理

反查规则与角色能力、道具/服装、地图拓扑、时间、文化、对白、分镜和提示词：
- 无行动/感知/概念作用：补可观察约束或降级，不自动加代价。
- 事实/机制矛盾：定位层级和当前依据，提最小修复，不静默覆盖。
- 视觉读不出：补一个有职责的物件、动作、空间或声源，不堆装饰。
- 世界过大：保留当前段落所需和必要完整性感，其他候选归档。
- 生成结果引入设定：登记提案，不自动批准。
- 上游改版：直接和传递受影响角色/场景/分镜/Prompt 标 needs_recheck；旧批准留历史但不作当前门禁。无关依赖经影响检查保留并说明理由。

## 9. 公共输出模板

生产交接/验证及明确要求登记的正式世界记录附以下公共头；讨论、探索、仅供阅读的局部规则/日常草稿不强制 YAML。正式成稿按指定范围交完整内容，不自动扩为世界圣经；内部表作为 payload，不另发明状态。未齐输入保持草案，达到公共协议候选条件才登记 candidate。

```yaml
artifact_id: WORLD-window
version: v1
artifact_type: world
creative_mode: perceptual
subject_type: environment-led
lifecycle_status: draft
production_status: not_applicable
approval_level: none
upstream_versions: {}
confirmed_evidence: []
open_questions: []
recheck_trigger: []
next_action: request_review
```

payload：character（无人物为 null）；基准/差异；规则表（机制/边界/适用代价与社会关系/行动或感知后果/证据）；感知目标/材料空间约束/可观察变化/完成判据，或概念命题/对照/指标/反例；母题权限/禁用能力；历史分支/依据；空间图/路径/时间；文化资源推导（适用时）；视觉语言；派生状态；禁用/不可推断项；下游接口；真实人工裁决与未决项。

交剧本：规则和适用选择压力或感知/概念操作。交角色（存在时）：身份、能力、材料、身体适应、日常/禁忌。交场景：拓扑、功能、边界、维护、基准/派生。交分镜/Prompt：可见证据、传播路径、状态变化、不可推断项和参考职责。候选可供探索，不能当硬约束；方向批准不等于生产批准、实际生成或自动入 Canon。

## 10. 最终闸门与来源入口

- 核心差异与风格分开，范围可控。
- 模式贯通，character: null 合法；规则有模式对应作用，不强要人物代价。
- 母题权限明确，没有从指示器偷偷升级为源头。
- 历史深度由已确认前提决定，日常不自动证明古史，梦境也不自动排除古史。
- 空间可达/传播、时间、资源和文化互相成立；证据可见可听。
- 基准可本来受损黑白，只有相对变化才派生。
- 已确认/候选/灵感分离，下游可读，版本和复核边界完整。

来源审计时加载 `references/source-map.md`；源版 skill/reference 仅供识别迁移来源，不是执行依赖，不伪造原书页码或未核验锚点。

---
